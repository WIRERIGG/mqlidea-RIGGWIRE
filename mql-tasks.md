# MQL5 Safety Tasks

*Auto-generated by RiggWire plugin — 2026-07-24 18:03:36*

**2732 open issues** — 0 errors, 1033 warnings, 1699 weak warnings

## Issue Summary

| Inspection | Count |
|---|---|
| ArrayAccessWithoutSizeCheck | 352 |
| MissingFunctionDocComment | 328 |
| FunctionNamingConvention | 324 |
| UnusedParameter | 211 |
| StackOverflowRisk | 209 |
| LargeStructByValue | 192 |
| ClassNamingConvention | 155 |
| OrderSelectUnchecked | 89 |
| DeepNesting | 82 |
| MissingTradeContextCheck | 71 |
| DataConsistency | 62 |
| MissingPropertyVersion | 60 |
| ReturnValueIgnored | 58 |
| ArrayResizeReturnCheck | 53 |
| TooManyParameters | 52 |
| SecureCodingPatterns | 43 |
| MissingPropertyDescription | 42 |
| MissingConstParameter | 40 |
| FileOperationValidation | 35 |
| MissingDefaultCase | 33 |
| MissingDestructor | 32 |
| MissingRefreshRates | 29 |
| OverComplexErrorHandling | 29 |
| UncheckedHandle | 25 |
| StringConcatInLoop | 23 |
| SafeApiUsage | 16 |
| AccountInfoExposure | 15 |
| NullAfterDelete | 11 |
| MissingOnInitReturn | 10 |
| PreprocessorProperty | 7 |
| TodoFixme | 6 |
| LongFunction | 5 |
| MissingArrayPreallocation | 4 |
| MissingIndicatorRelease | 4 |
| DeleteWithoutNullCheck | 3 |
| PrintInOnTick | 3 |
| IncompleteClass | 2 |
| MissingBreakInSwitch | 2 |
| MissingErrorRecovery | 2 |
| UncheckedCopyRates | 2 |
| ArrayResizeInLoop | 1 |
| DoubleIndicatorRelease | 1 |
| EmptyLoopBody | 1 |
| LazyEvaluationMiss | 1 |
| MissingFileClose | 1 |
| ModernMQL5Idiom | 1 |
| NewKeywordInLoop | 1 |
| OrderCloseLoopDirection | 1 |
| RedundantCalculationInOnTick | 1 |
| SuspiciousSemicolon | 1 |
| UnsafeArrayCopy | 1 |

## Tasks by File

### src/test/resources/samples/Include/Indicators/Oscilators.mqh (147 issues)

- [ ] **Line 1** !! Indicator handle created but IndicatorRelease() not found in OnDeinit — potential memory leak `MissingIndicatorRelease`
  - Fix: Add IndicatorRelease(handle) in OnDeinit()
- [ ] **Line 50** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 59** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 59** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 59** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 66** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 77** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 124** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 133** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 133** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 133** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 140** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 151** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 205** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 214** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 214** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 214** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 221** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 232** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 289** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 298** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 298** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 298** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 305** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 317** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 371** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 380** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 380** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 380** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 387** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 398** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 460** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 461** !! User-defined type 'ENUM_MA_METHOD' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 461** !! User-defined type 'ENUM_APPLIED_PRICE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 470** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 470** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 470** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 478** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 479** !! User-defined type 'ENUM_MA_METHOD' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 479** !! User-defined type 'ENUM_APPLIED_PRICE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 492** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 561** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 572** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 572** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 572** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 580** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 596** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 662** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 671** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 671** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 671** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 678** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 690** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 759** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 770** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 770** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 770** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 778** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 795** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 853** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 862** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 862** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 862** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 869** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 881** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 936** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 945** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 945** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 945** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 952** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1041** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1043** !! User-defined type 'ENUM_MA_METHOD' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1043** !! User-defined type 'ENUM_STO_PRICE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1052** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1052** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1052** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 1061** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1063** !! User-defined type 'ENUM_MA_METHOD' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1063** !! User-defined type 'ENUM_STO_PRICE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1078** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 1139** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1148** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1148** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1148** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 1155** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1166** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 12** ! Class 'CiATR' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 59** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 66** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 66** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 86** ! Class 'CiBearsPower' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 133** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 140** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 140** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 151** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 167** ! Class 'CiBullsPower' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 214** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 221** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 221** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 232** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 248** ! Class 'CiCCI' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 298** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 305** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 305** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 317** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 333** ! Class 'CiDeMarker' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 380** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 387** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 387** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 398** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 414** ! Class 'CiForce' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 470** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 478** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 478** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 492** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 509** ! Class 'CiMACD' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 572** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 580** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 580** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 621** ! Class 'CiMomentum' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 671** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 678** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 678** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 690** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 707** ! Class 'CiOsMA' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 770** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 778** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 778** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 795** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 812** ! Class 'CiRSI' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 862** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 869** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 869** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 881** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 897** ! Class 'CiRVI' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 945** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 952** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 952** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 986** ! Class 'CiStochastic' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 1052** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 1061** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 1061** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 1101** ! Class 'CiWPR' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 1148** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 1155** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 1155** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 1166** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/parser/functions/FunctionDefinition.mq4 (111 issues)

- [ ] **Line 59** !! User-defined type 'CustomType' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 3** ! Contains 'TODO' marker — address before production deployment `TodoFixme`
  - Fix: Resolve the TODO/FIXME item or create a tracking issue
- [ ] **Line 4** ! Function 'z2' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 5** ! Function 'z3' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 5** ! Public function 'z3()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 6** ! Function 'z4' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 6** ! Public function 'z4()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 7** ! Function 'z5' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 7** ! Public function 'z5()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 8** ! Function 'z6' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 8** ! Public function 'z6()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 9** ! Function 'z7' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 9** ! Public function 'z7()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 10** ! Function 'z8' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 10** ! Public function 'z8()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 11** ! Function 'z9' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 11** ! Public function 'z9()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 12** ! Function 'z10' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 12** ! Public function 'z10()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 13** ! Function 'z100' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 13** ! Public function 'z100()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 19** ! Function 'a2' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 19** ! Public function 'a2()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 23** ! Function 'a3' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 23** ! Function 'a4' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 23** ! Public function 'a3()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 23** ! Public function 'a4()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 26** ! Function 'b1' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 26** ! Parameter 'x' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 27** ! Function 'b2' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 27** ! Public function 'b2()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 27** ! Parameter 'x' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 27** ! Parameter 'y' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 30** ! Function 'c1' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 30** ! Parameter 'x' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 31** ! Function 'c2' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 31** ! Public function 'c2()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 31** ! Parameter 'x' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 31** ! Parameter 'y' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 32** ! Function 'c3' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 32** ! Public function 'c3()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 32** ! Parameter 's' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 33** ! Contains 'TODO' marker — address before production deployment `TodoFixme`
  - Fix: Resolve the TODO/FIXME item or create a tracking issue
- [ ] **Line 34** ! Contains 'TODO' marker — address before production deployment `TodoFixme`
  - Fix: Resolve the TODO/FIXME item or create a tracking issue
- [ ] **Line 36** ! Function 'c6' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 36** ! Public function 'c6()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 36** ! Parameter 'X1' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 39** ! Function 'd1' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 40** ! Function 'd2' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 40** ! Public function 'd2()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 41** ! Function 'd3' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 41** ! Public function 'd3()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 41** ! Parameter 'c' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 42** ! Function 'd4' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 42** ! Public function 'd4()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 42** ! Parameter 'b' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 43** ! Function 'd5' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 43** ! Public function 'd5()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 43** ! Parameter 'b' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 46** ! Function 'e1' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 46** ! Parameter 'x' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 47** ! Function 'e2' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 47** ! Public function 'e2()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 47** ! Parameter 'y' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 48** ! Function 'e3' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 48** ! Public function 'e3()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 49** ! Function 'e4' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 49** ! Public function 'e4()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 50** ! Function 'e5' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 50** ! Public function 'e5()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 50** ! Parameter 'x' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 51** ! Function 'e6' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 51** ! Public function 'e6()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 51** ! Parameter 'y' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 52** ! Function 'e7' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 52** ! Public function 'e7()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 52** ! Parameter 'z' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 53** ! Function 'e8' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 53** ! Public function 'e8()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 53** ! Parameter 'a' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 54** ! Function 'e9' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 54** ! Public function 'e9()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 55** ! Function 'e10' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 55** ! Public function 'e10()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 55** ! Parameter 'x' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 56** ! Function 'e10' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 56** ! Public function 'e10()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 56** ! Parameter 'x' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 58** ! Function 'e11' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 58** ! Public function 'e11()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 58** ! Parameter 'ARRAY_SIZE' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 59** ! Function 'e12' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 59** ! Public function 'e12()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 59** ! Parameter 'x' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 60** ! Function 'e13' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 60** ! Public function 'e13()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 61** ! Function 'e14' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 61** ! Public function 'e14()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 62** ! Function 'e15' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 62** ! Public function 'e15()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 63** ! Function 'e16' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 63** ! Public function 'e16()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 64** ! Function 'e17' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 64** ! Public function 'e17()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 64** ! Parameter 'x' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 72** ! Function 'm1' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 73** ! Function 'm2' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 73** ! Public function 'm2()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 75** ! Public function 'SomeClass()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/opensource/misc/UnitTest.mqh (91 issues)

- [ ] **Line 14** !! Class 'UnitTestData' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 110** !! Suspicious semicolon after control statement — likely a logic error (empty body) `SuspiciousSemicolon`
  - Fix: Remove suspicious semicolon after if/for/while, or add braces for empty body
- [ ] **Line 14** ! Class 'UnitTestData' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 25** ! Public function 'UnitTestData()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 25** ! Parameter 'name' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 30** ! Class 'UnitTest' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 86** ! Public function 'UnitTest()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 96** ! Function 'addTest' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 96** ! Public function 'addTest()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 107** ! Function 'clearTestList' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 107** ! Public function 'clearTestList()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 110** ! Empty loop body — possible missing implementation `EmptyLoopBody`
  - Fix: Insert TODO comment in empty loop body
- [ ] **Line 115** ! Function 'findTest' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 115** ! Public function 'findTest()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 127** ! Function 'setSuccess' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 127** ! Public function 'setSuccess()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 144** ! Function 'setFailure' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 144** ! Public function 'setFailure()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 158** ! Function 'printSummary' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 158** ! Public function 'printSummary()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 181** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 181** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 194** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 194** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 207** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 207** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 220** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 220** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 233** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 233** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 246** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 246** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 259** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 259** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 272** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 272** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 285** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 285** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 298** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 298** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 311** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 311** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 324** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 324** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 337** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 337** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 350** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 350** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 363** ! Function 'assertArraySize' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 363** ! Public function 'assertArraySize()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 378** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 378** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 387** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 401** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 401** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 410** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 424** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 424** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 433** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 447** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 447** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 456** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 470** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 470** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 479** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 493** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 493** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 502** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 516** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 516** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 525** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 539** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 539** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 548** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 562** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 562** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 571** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 585** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 585** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 594** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 608** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 608** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 617** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 631** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 631** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 640** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 655** ! Function 'assertEquals' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 655** ! Public function 'assertEquals()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 664** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 678** ! Function 'fail' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 678** ! Public function 'fail()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/opensource/Ilan/Double(Ilan1_5&Ilan1_6).mq4 (88 issues)

- [ ] **Line 116** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 116** !! Account information sent to external output — potential data privacy risk `AccountInfoExposure`
  - Fix: Avoid logging sensitive account info; use masked output if needed
- [ ] **Line 504** !! OrderSend() without volume validation — validate lot size before sending orders `SafeApiUsage`
  - Fix: Add volume validation: double lot = NormalizeDouble(volume, 2); check against SymbolInfoDouble()
- [ ] **Line 509** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 703** !! OrderSend() without volume validation — validate lot size before sending orders `SafeApiUsage`
  - Fix: Add volume validation: double lot = NormalizeDouble(volume, 2); check against SymbolInfoDouble()
- [ ] **Line 708** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 103** ! Function 'init' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 110** ! Function 'deinit' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 110** ! Public function 'deinit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 116** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 116** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 116** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 116** ! Public function 'start()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 116** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 116** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 116** ! Function 'start' is 360 lines long — consider decomposing into smaller functions `LongFunction`
  - Fix: Split long function into smaller, focused functions
- [ ] **Line 480** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 491** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 491** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 491** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 491** ! Public function 'CloseThisSymbolAll()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 491** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 491** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 504** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 504** ! Public function 'OpenPendingOrder()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 504** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 504** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 504** ! Function 'OpenPendingOrder' has 11 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 568** ! Public function 'StopLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 573** ! Public function 'StopShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 578** ! Public function 'TakeLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 583** ! Public function 'TakeShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 588** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 588** ! Public function 'CalculateProfit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 599** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 599** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 599** ! Public function 'TrailingAlls()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 599** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 629** ! Public function 'AccountEquityHigh()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 637** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 637** ! Public function 'FindLastBuyPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 637** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 657** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 657** ! Public function 'FindLastSellPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 657** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 679** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 679** ! Function 'CountTrades_16' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 690** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 690** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 690** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 690** ! Function 'CloseThisSymbolAll_16' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 690** ! Public function 'CloseThisSymbolAll_16()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 690** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 690** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 703** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 703** ! Function 'OpenPendingOrder_16' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 703** ! Public function 'OpenPendingOrder_16()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 703** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 703** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 703** ! Function 'OpenPendingOrder_16' has 11 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 767** ! Function 'StopLong_16' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 767** ! Public function 'StopLong_16()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 772** ! Function 'StopShort_16' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 772** ! Public function 'StopShort_16()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 777** ! Function 'TakeLong_16' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 777** ! Public function 'TakeLong_16()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 782** ! Function 'TakeShort_16' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 782** ! Public function 'TakeShort_16()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 787** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 787** ! Function 'CalculateProfit_16' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 787** ! Public function 'CalculateProfit_16()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 798** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 798** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 798** ! Function 'TrailingAlls_16' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 798** ! Public function 'TrailingAlls_16()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 798** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 828** ! Function 'AccountEquityHigh_16' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 828** ! Public function 'AccountEquityHigh_16()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 836** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 836** ! Function 'FindLastBuyPrice_16' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 836** ! Public function 'FindLastBuyPrice_16()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 836** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 856** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 856** ! Function 'FindLastSellPrice_16' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 856** ! Public function 'FindLastSellPrice_16()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 856** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)

### src/test/resources/samples/Include/Indicators/Trend.mqh (80 issues)

- [ ] **Line 1** !! Indicator handle created but IndicatorRelease() not found in OnDeinit — potential memory leak `MissingIndicatorRelease`
  - Fix: Add IndicatorRelease(handle) in OnDeinit()
- [ ] **Line 52** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 61** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 61** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 61** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 68** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 79** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 162** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 173** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 173** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 173** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 181** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 197** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 282** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 283** !! User-defined type 'ENUM_MA_METHOD' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 293** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 293** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 293** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 302** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 303** !! User-defined type 'ENUM_MA_METHOD' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 319** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 392** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 402** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 402** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 402** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 409** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 423** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 519** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 521** !! User-defined type 'ENUM_MA_METHOD' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 530** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 530** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 530** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 538** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 540** !! User-defined type 'ENUM_MA_METHOD' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 554** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 612** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 621** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 621** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 621** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 628** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 641** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 708** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 710** !! User-defined type 'ENUM_MA_METHOD' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 719** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 719** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 719** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 727** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 729** !! User-defined type 'ENUM_MA_METHOD' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 743** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 12** ! Class 'CiADX' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 61** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 68** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 68** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 109** ! Class 'CiBands' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 173** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 181** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 181** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 227** ! Class 'CiEnvelopes' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 293** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 302** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 302** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 342** ! Class 'CiIchimoku' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 402** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 409** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 409** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 467** ! Class 'CiMA' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 530** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 538** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 538** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 554** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 571** ! Class 'CiSAR' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 621** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 628** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 628** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 641** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 657** ! Class 'CiStdDev' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 719** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 727** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 727** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 743** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/Include/Indicators/BillWilliams.mqh (73 issues)

- [ ] **Line 1** !! Indicator handle created but IndicatorRelease() not found in OnDeinit — potential memory leak `MissingIndicatorRelease`
  - Fix: Add IndicatorRelease(handle) in OnDeinit()
- [ ] **Line 45** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 54** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 54** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 61** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 70** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 155** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 159** !! User-defined type 'ENUM_MA_METHOD' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 168** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 168** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 168** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 178** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 182** !! User-defined type 'ENUM_MA_METHOD' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 200** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 267** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 276** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 276** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 283** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 292** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 342** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 351** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 351** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 358** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 367** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 458** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 462** !! User-defined type 'ENUM_MA_METHOD' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 471** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 471** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 471** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 481** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 485** !! User-defined type 'ENUM_MA_METHOD' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 503** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 569** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 569** !! User-defined type 'ENUM_APPLIED_VOLUME' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 578** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 578** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 578** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 585** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 585** !! User-defined type 'ENUM_APPLIED_VOLUME' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 12** ! Class 'CiAC' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 54** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 54** ! Parameter 'params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 61** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 61** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 70** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 86** ! Class 'CiAlligator' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 155** ! Function 'Create' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 168** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 178** ! Function 'Initialize' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 178** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 178** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 234** ! Class 'CiAO' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 276** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 276** ! Parameter 'params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 283** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 283** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 292** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 308** ! Class 'CiFractals' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 351** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 351** ! Parameter 'params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 358** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 358** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 390** ! Class 'CiGator' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 458** ! Function 'Create' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 471** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 481** ! Function 'Initialize' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 481** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 481** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 531** ! Class 'CiBWMFI' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 578** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 585** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 585** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 596** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/Include/Canvas/Canvas.mqh (61 issues)

- [ ] **Line 183** !! User-defined type 'ENUM_COLOR_FORMAT' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 183** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 212** !! Function 'CreateBitmap()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 213** !! User-defined type 'ENUM_COLOR_FORMAT' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 222** !! User-defined type 'ENUM_COLOR_FORMAT' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 250** !! Function 'CreateBitmapLabel()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 251** !! User-defined type 'ENUM_COLOR_FORMAT' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 260** !! User-defined type 'ENUM_COLOR_FORMAT' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 295** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 327** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 359** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 370** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 379** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 439** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 489** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 606** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 672** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 787** !! Function 'Arc()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 827** !! Function 'Arc()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 1547** !! Function 'Pie()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 1848** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 1858** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 1893** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 2209** !! Function 'TextOut()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 2258** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 2258** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 2314** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 2315** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 2316** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 2479** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 2486** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 2491** !! ArrayResize() inside loop — pre-allocate array before loop for better performance `ArrayResizeInLoop`
  - Fix: Move ArrayResize() before the loop and pre-allocate with reserve parameter
- [ ] **Line 2503** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 2505** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 2506** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 2510** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 2511** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 2512** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 2513** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 2522** !! Function 'PolygonFill()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 220** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 220** ! Function 'CreateBitmap' has 8 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 258** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 258** ! Function 'CreateBitmapLabel' has 8 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 327** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 787** ! Function 'Arc' has 9 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 880** ! Excessive nesting depth (8 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 880** ! Function 'Arc' is 663 lines long — consider decomposing into smaller functions `LongFunction`
  - Fix: Split long function into smaller, focused functions
- [ ] **Line 880** ! Function 'Arc' has 11 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 1547** ! Function 'Pie' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 1588** ! Function 'Pie' has 8 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 2068** ! Function 'TriangleAA' has 8 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 2104** ! Public function 'GetDefaultColor()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 2258** ! ArrayCopy() without size validation — verify source and destination sizes `UnsafeArrayCopy`
  - Fix: Validate source/destination sizes before ArrayCopy()
- [ ] **Line 2258** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 2430** ! Parameter 'p' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 2449** ! Parameter 'p' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 2486** ! Parameter 'p' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 2486** ! Parameter 'add' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 2486** ! Multiple incremental ArrayResize() calls — use reserve parameter for pre-allocation `MissingArrayPreallocation`
  - Fix: Pre-allocate array: ArrayResize(arr, expectedSize, reserveSize)
- [ ] **Line 2522** ! Parameter 'p' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param

### src/test/resources/samples/opensource/Ilan/Ilan1.4.mq4 (58 issues)

- [ ] **Line 66** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 66** !! Account information sent to external output — potential data privacy risk `AccountInfoExposure`
  - Fix: Avoid logging sensitive account info; use masked output if needed
- [ ] **Line 298** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 349** !! OrderSend() without volume validation — validate lot size before sending orders `SafeApiUsage`
  - Fix: Add volume validation: double lot = NormalizeDouble(volume, 2); check against SymbolInfoDouble()
- [ ] **Line 354** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 57** ! Function 'init' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 57** ! Public function 'init()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 62** ! Function 'deinit' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 62** ! Public function 'deinit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 66** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 66** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 66** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 66** ! Public function 'start()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 66** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 66** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 250** ! Public function 'ND()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 254** ! Function 'fOrderCloseMarket' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 254** ! Public function 'fOrderCloseMarket()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 254** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 254** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 254** ! Parameter 'TRUE' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 254** ! Parameter 'TRUE' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 256** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 295** ! Function 'fGetLots' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 295** ! Public function 'fGetLots()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 295** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 325** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 325** ! Public function 'CountTrades()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 336** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 336** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 336** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 336** ! Public function 'CloseThisSymbolAll()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 336** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 336** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 349** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 349** ! Public function 'OpenPendingOrder()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 349** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 349** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 349** ! Function 'OpenPendingOrder' has 11 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 413** ! Public function 'StopLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 418** ! Public function 'StopShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 423** ! Public function 'TakeLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 428** ! Public function 'TakeShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 433** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 433** ! Public function 'CalculateProfit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 444** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 444** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 444** ! Public function 'TrailingAlls()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 444** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 474** ! Public function 'AccountEquityHigh()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 482** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 482** ! Public function 'FindLastBuyPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 482** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 502** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 502** ! Public function 'FindLastSellPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 502** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)

### src/test/resources/samples/opensource/misc/RSI Envelopes EA.mq4 (57 issues)

- [ ] **Line 213** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 254** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 254** !! 'delete' used without prior NULL check — risk of use-after-free `DeleteWithoutNullCheck`
  - Fix: Add NULL check before delete: if(ptr != NULL) { delete ptr; ptr = NULL; }
- [ ] **Line 254** !! Account information sent to external output — potential data privacy risk `AccountInfoExposure`
  - Fix: Avoid logging sensitive account info; use masked output if needed
- [ ] **Line 727** !! OrderSend() without volume validation — validate lot size before sending orders `SafeApiUsage`
  - Fix: Add volume validation: double lot = NormalizeDouble(volume, 2); check against SymbolInfoDouble()
- [ ] **Line 758** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 213** ! Function 'init' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 254** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 254** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 254** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 254** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 254** ! Function 'start' is 468 lines long — consider decomposing into smaller functions `LongFunction`
  - Fix: Split long function into smaller, focused functions
- [ ] **Line 727** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 727** ! Function 'open' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 747** ! Function 'lotsoptimized' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 758** ! Function 'checktime' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 815** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 815** ! Function 'count' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 830** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 830** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 830** ! Function 'close' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 830** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 848** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 848** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 848** ! Function 'partialclose' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 848** ! Public function 'partialclose()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 848** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 866** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 866** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 866** ! Function 'hideclose' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 866** ! Public function 'hideclose()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 866** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 888** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 888** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 888** ! Function 'closetime' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 888** ! Public function 'closetime()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 888** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 935** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 935** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 935** ! Function 'movebreakeven' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 935** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 962** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 962** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 962** ! Function 'movetrailingstop' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 962** ! Public function 'movetrailingstop()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 962** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 1019** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 1019** ! Function 'movetrailingprofit' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 1019** ! Public function 'movetrailingprofit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 1019** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 1048** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 1048** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 1048** ! Function 'ecnmodify' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 1048** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 1083** ! Function 'errordescription' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/opensource/Ilan/Ilan1.6_BBPower_v2.2Time.mq4 (50 issues)

- [ ] **Line 71** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 71** !! Account information sent to external output — potential data privacy risk `AccountInfoExposure`
  - Fix: Avoid logging sensitive account info; use masked output if needed
- [ ] **Line 287** !! OrderSend() without volume validation — validate lot size before sending orders `SafeApiUsage`
  - Fix: Add volume validation: double lot = NormalizeDouble(volume, 2); check against SymbolInfoDouble()
- [ ] **Line 292** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 472** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 62** ! Function 'init' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 62** ! Public function 'init()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 67** ! Function 'deinit' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 67** ! Public function 'deinit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 71** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 71** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 71** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 71** ! Public function 'start()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 71** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 71** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 263** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 263** ! Public function 'CountTrades()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 274** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 274** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 274** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 274** ! Public function 'CloseThisSymbolAll()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 274** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 274** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 287** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 287** ! Public function 'OpenPendingOrder()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 287** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 287** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 287** ! Function 'OpenPendingOrder' has 11 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 351** ! Public function 'StopLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 356** ! Public function 'StopShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 361** ! Public function 'TakeLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 366** ! Public function 'TakeShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 371** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 371** ! Public function 'CalculateProfit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 382** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 382** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 382** ! Public function 'TrailingAlls()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 382** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 412** ! Public function 'AccountEquityHigh()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 420** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 420** ! Public function 'FindLastBuyPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 420** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 440** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 440** ! Public function 'FindLastSellPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 440** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 460** ! Public function 'GetLots()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 472** ! Function 'again' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 472** ! Public function 'again()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/opensource/Ilan/Ilan16_.mq4 (46 issues)

- [ ] **Line 61** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 61** !! Account information sent to external output — potential data privacy risk `AccountInfoExposure`
  - Fix: Avoid logging sensitive account info; use masked output if needed
- [ ] **Line 250** !! OrderSend() without volume validation — validate lot size before sending orders `SafeApiUsage`
  - Fix: Add volume validation: double lot = NormalizeDouble(volume, 2); check against SymbolInfoDouble()
- [ ] **Line 255** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 52** ! Function 'init' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 52** ! Public function 'init()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 57** ! Function 'deinit' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 57** ! Public function 'deinit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 61** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 61** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 61** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 61** ! Public function 'start()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 61** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 61** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 226** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 226** ! Public function 'CountTrades()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 237** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 237** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 237** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 237** ! Public function 'CloseThisSymbolAll()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 237** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 237** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 250** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 250** ! Public function 'OpenPendingOrder()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 250** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 250** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 250** ! Function 'OpenPendingOrder' has 11 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 314** ! Public function 'StopLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 319** ! Public function 'StopShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 324** ! Public function 'TakeLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 329** ! Public function 'TakeShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 334** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 334** ! Public function 'CalculateProfit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 345** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 345** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 345** ! Public function 'TrailingAlls()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 345** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 375** ! Public function 'AccountEquityHigh()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 383** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 383** ! Public function 'FindLastBuyPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 383** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 403** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 403** ! Public function 'FindLastSellPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 403** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)

### src/test/resources/samples/opensource/Ilan/Ilan_1.6.mq4 (46 issues)

- [ ] **Line 61** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 61** !! Account information sent to external output — potential data privacy risk `AccountInfoExposure`
  - Fix: Avoid logging sensitive account info; use masked output if needed
- [ ] **Line 250** !! OrderSend() without volume validation — validate lot size before sending orders `SafeApiUsage`
  - Fix: Add volume validation: double lot = NormalizeDouble(volume, 2); check against SymbolInfoDouble()
- [ ] **Line 255** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 52** ! Function 'init' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 52** ! Public function 'init()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 57** ! Function 'deinit' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 57** ! Public function 'deinit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 61** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 61** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 61** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 61** ! Public function 'start()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 61** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 61** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 226** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 226** ! Public function 'CountTrades()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 237** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 237** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 237** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 237** ! Public function 'CloseThisSymbolAll()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 237** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 237** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 250** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 250** ! Public function 'OpenPendingOrder()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 250** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 250** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 250** ! Function 'OpenPendingOrder' has 11 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 314** ! Public function 'StopLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 319** ! Public function 'StopShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 324** ! Public function 'TakeLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 329** ! Public function 'TakeShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 334** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 334** ! Public function 'CalculateProfit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 345** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 345** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 345** ! Public function 'TrailingAlls()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 345** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 375** ! Public function 'AccountEquityHigh()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 383** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 383** ! Public function 'FindLastBuyPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 383** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 403** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 403** ! Public function 'FindLastSellPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 403** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)

### src/test/resources/samples/opensource/mql4-lib/Collection/HashMap.mqh (46 issues)

- [ ] **Line 20** !! Class 'HashMap' uses new/delete but lacks copy constructor or assignment operator (Rule of Three) `IncompleteClass`
  - Fix: Implement copy constructor and assignment operator, or delete them
- [ ] **Line 69** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 74** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 76** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 77** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 78** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 84** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 105** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 108** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 151** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 173** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 173** !! User-defined type 'Key' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 200** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 217** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 234** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 234** !! User-defined type 'Key' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 234** !! User-defined type 'Value' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 234** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 241** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 242** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 243** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 264** !! Class 'HashMapIterator' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 13** ! Class 'HashMapIterator' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 20** ! Class 'HashMap' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 69** ! Function 'initState' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 69** ! Public function 'initState()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 69** ! Multiple incremental ArrayResize() calls — use reserve parameter for pre-allocation `MissingArrayPreallocation`
  - Fix: Pre-allocate array: ArrayResize(arr, expectedSize, reserveSize)
- [ ] **Line 84** ! Function 'clearEntry' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 84** ! Public function 'clearEntry()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 105** ! Function 'upsize' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 105** ! Public function 'upsize()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 129** ! Public function 'HashMap()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 151** ! Function 'lookup' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 151** ! Public function 'lookup()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 173** ! Function 'remove' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 173** ! Public function 'remove()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 188** ! Function 'clear' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 188** ! Public function 'clear()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 200** ! Function 'keys' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 200** ! Public function 'keys()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 217** ! Function 'values' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 217** ! Public function 'values()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 234** ! Function 'set' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 234** ! Public function 'set()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 234** ! Multiple incremental ArrayResize() calls — use reserve parameter for pre-allocation `MissingArrayPreallocation`
  - Fix: Pre-allocate array: ArrayResize(arr, expectedSize, reserveSize)
- [ ] **Line 264** ! Class 'HashMapIterator' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/opensource/Ilan/Ilan5_MT4build2.mq4 (46 issues)

- [ ] **Line 61** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 61** !! Account information sent to external output — potential data privacy risk `AccountInfoExposure`
  - Fix: Avoid logging sensitive account info; use masked output if needed
- [ ] **Line 246** !! OrderSend() without volume validation — validate lot size before sending orders `SafeApiUsage`
  - Fix: Add volume validation: double lot = NormalizeDouble(volume, 2); check against SymbolInfoDouble()
- [ ] **Line 251** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 52** ! Function 'init' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 52** ! Public function 'init()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 57** ! Function 'deinit' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 57** ! Public function 'deinit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 61** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 61** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 61** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 61** ! Public function 'start()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 61** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 61** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 222** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 222** ! Public function 'CountTrades()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 233** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 233** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 233** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 233** ! Public function 'CloseThisSymbolAll()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 233** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 233** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 246** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 246** ! Public function 'OpenPendingOrder()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 246** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 246** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 246** ! Function 'OpenPendingOrder' has 11 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 310** ! Public function 'StopLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 315** ! Public function 'StopShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 320** ! Public function 'TakeLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 325** ! Public function 'TakeShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 330** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 330** ! Public function 'CalculateProfit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 341** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 341** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 341** ! Public function 'TrailingAlls()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 341** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 371** ! Public function 'AccountEquityHigh()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 379** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 379** ! Public function 'FindLastBuyPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 379** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 399** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 399** ! Public function 'FindLastSellPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 399** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)

### src/test/resources/samples/Include/Arrays/ArrayObj.mqh (43 issues)

- [ ] **Line 88** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 88** !! 'delete m_data' without subsequent 'm_data = NULL' — leaves dangling pointer `NullAfterDelete`
  - Fix: Set pointer to NULL after delete: delete ptr; ptr = NULL;
- [ ] **Line 138** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 163** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 163** !! 'delete m_data' without subsequent 'm_data = NULL' — leaves dangling pointer `NullAfterDelete`
  - Fix: Set pointer to NULL after delete: delete ptr; ptr = NULL;
- [ ] **Line 194** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 210** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 227** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 247** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 271** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 291** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 320** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 331** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 331** !! 'delete m_data' without subsequent 'm_data = NULL' — leaves dangling pointer `NullAfterDelete`
  - Fix: Set pointer to NULL after delete: delete ptr; ptr = NULL;
- [ ] **Line 349** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 372** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 372** !! 'delete m_data' without subsequent 'm_data = NULL' — leaves dangling pointer `NullAfterDelete`
  - Fix: Set pointer to NULL after delete: delete ptr; ptr = NULL;
- [ ] **Line 393** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 412** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 412** !! 'delete m_data' without subsequent 'm_data = NULL' — leaves dangling pointer `NullAfterDelete`
  - Fix: Set pointer to NULL after delete: delete ptr; ptr = NULL;
- [ ] **Line 432** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 432** !! 'delete m_data' without subsequent 'm_data = NULL' — leaves dangling pointer `NullAfterDelete`
  - Fix: Set pointer to NULL after delete: delete ptr; ptr = NULL;
- [ ] **Line 449** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 466** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 466** !! Function 'QuickSort()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 514** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 544** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 571** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 588** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 606** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 624** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 640** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 655** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 676** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 697** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 697** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 716** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 716** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 163** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 320** ! Public function 'At()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 393** ! Public function 'Detach()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 466** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 716** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations

### src/test/resources/samples/opensource/Ilan/Ilan1.6.Prof6.mq4 (41 issues)

- [ ] **Line 48** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 48** !! Account information sent to external output — potential data privacy risk `AccountInfoExposure`
  - Fix: Avoid logging sensitive account info; use masked output if needed
- [ ] **Line 241** !! OrderSend() without volume validation — validate lot size before sending orders `SafeApiUsage`
  - Fix: Add volume validation: double lot = NormalizeDouble(volume, 2); check against SymbolInfoDouble()
- [ ] **Line 246** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 39** ! Function 'init' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 44** ! Function 'deinit' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 44** ! Public function 'deinit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 48** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 48** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 48** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 48** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 48** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 214** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 226** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 226** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 226** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 226** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 226** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 241** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 241** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 241** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 241** ! Function 'OpenPendingOrder' has 11 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 305** ! Public function 'StopLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 310** ! Public function 'StopShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 315** ! Public function 'TakeLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 320** ! Public function 'TakeShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 325** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 325** ! Public function 'CalculateProfit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 336** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 336** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 336** ! Public function 'TrailingAlls()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 336** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 366** ! Public function 'AccountEquityHigh()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 374** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 374** ! Public function 'FindLastBuyPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 374** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 394** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 394** ! Public function 'FindLastSellPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 394** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)

### src/test/resources/samples/opensource/Ilan/Ilan_MA.mq4 (41 issues)

- [ ] **Line 52** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 52** !! Account information sent to external output — potential data privacy risk `AccountInfoExposure`
  - Fix: Avoid logging sensitive account info; use masked output if needed
- [ ] **Line 253** !! OrderSend() without volume validation — validate lot size before sending orders `SafeApiUsage`
  - Fix: Add volume validation: double lot = NormalizeDouble(volume, 2); check against SymbolInfoDouble()
- [ ] **Line 258** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 43** ! Function 'init' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 48** ! Function 'deinit' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 48** ! Public function 'deinit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 52** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 52** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 52** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 52** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 52** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 226** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 238** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 238** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 238** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 238** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 238** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 253** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 253** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 253** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 253** ! Function 'OpenPendingOrder' has 11 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 317** ! Public function 'StopLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 322** ! Public function 'StopShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 327** ! Public function 'TakeLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 332** ! Public function 'TakeShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 337** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 337** ! Public function 'CalculateProfit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 348** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 348** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 348** ! Public function 'TrailingAlls()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 348** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 378** ! Public function 'AccountEquityHigh()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 386** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 386** ! Public function 'FindLastBuyPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 386** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 406** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 406** ! Public function 'FindLastSellPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 406** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)

### src/test/resources/samples/opensource/Ilan/Ilan_RSI.mq4 (41 issues)

- [ ] **Line 48** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 48** !! Account information sent to external output — potential data privacy risk `AccountInfoExposure`
  - Fix: Avoid logging sensitive account info; use masked output if needed
- [ ] **Line 241** !! OrderSend() without volume validation — validate lot size before sending orders `SafeApiUsage`
  - Fix: Add volume validation: double lot = NormalizeDouble(volume, 2); check against SymbolInfoDouble()
- [ ] **Line 246** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 39** ! Function 'init' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 44** ! Function 'deinit' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 44** ! Public function 'deinit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 48** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 48** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 48** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 48** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 48** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 214** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 226** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 226** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 226** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 226** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 226** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 241** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 241** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 241** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 241** ! Function 'OpenPendingOrder' has 11 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 305** ! Public function 'StopLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 310** ! Public function 'StopShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 315** ! Public function 'TakeLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 320** ! Public function 'TakeShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 325** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 325** ! Public function 'CalculateProfit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 336** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 336** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 336** ! Public function 'TrailingAlls()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 336** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 366** ! Public function 'AccountEquityHigh()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 374** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 374** ! Public function 'FindLastBuyPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 374** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 394** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 394** ! Public function 'FindLastSellPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 394** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)

### src/test/resources/samples/opensource/Ilan/IlanRSI_Akzist.mq4 (41 issues)

- [ ] **Line 53** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 53** !! Account information sent to external output — potential data privacy risk `AccountInfoExposure`
  - Fix: Avoid logging sensitive account info; use masked output if needed
- [ ] **Line 260** !! Account information sent to external output — potential data privacy risk `AccountInfoExposure`
  - Fix: Avoid logging sensitive account info; use masked output if needed
- [ ] **Line 265** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 43** ! Function 'init' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 48** ! Function 'deinit' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 48** ! Public function 'deinit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 53** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 53** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 53** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 53** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 53** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 233** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 245** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 245** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 245** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 245** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 245** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 260** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 260** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 260** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 260** ! Function 'OpenPendingOrder' has 11 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 372** ! Public function 'StopLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 377** ! Public function 'StopShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 382** ! Public function 'TakeLong()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 387** ! Public function 'TakeShort()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 392** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 392** ! Public function 'CalculateProfit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 403** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 403** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 403** ! Public function 'TrailingAlls()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 403** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 433** ! Public function 'AccountEquityHigh()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 441** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 441** ! Public function 'FindLastBuyPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 441** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 461** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 461** ! Public function 'FindLastSellPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 461** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)

### src/test/resources/samples/Include/Indicators/Volumes.mqh (36 issues)

- [ ] **Line 1** !! Indicator handle created but IndicatorRelease() not found in OnDeinit — potential memory leak `MissingIndicatorRelease`
  - Fix: Add IndicatorRelease(handle) in OnDeinit()
- [ ] **Line 50** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 50** !! User-defined type 'ENUM_APPLIED_VOLUME' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 59** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 59** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 59** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 66** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 66** !! User-defined type 'ENUM_APPLIED_VOLUME' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 131** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 140** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 140** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 140** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 147** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 212** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 212** !! User-defined type 'ENUM_APPLIED_PRICE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 221** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 221** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 221** !! Function 'Initialize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 228** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 228** !! User-defined type 'ENUM_APPLIED_PRICE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 239** !! Indicator handle should be checked against INVALID_HANDLE after creation `UncheckedHandle`
  - Fix: Check handle after creation: if(handle == INVALID_HANDLE) { Print("Error: ", GetLastError()); }
- [ ] **Line 12** ! Class 'CiAD' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 59** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 66** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 66** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 77** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 93** ! Class 'CiMFI' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 140** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 147** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 147** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 158** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 174** ! Class 'CiOBV' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 221** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 228** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 228** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 239** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/Include/ChartObjects/ChartObjectsTxtControls.mqh (33 issues)

- [ ] **Line 144** !! User-defined type 'ENUM_ANCHOR_POINT' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 155** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 187** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 354** !! User-defined type 'ENUM_BASE_CORNER' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 365** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 389** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 573** !! User-defined type 'ENUM_ALIGN_MODE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 584** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 608** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 685** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 703** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 833** !! User-defined type 'ENUM_BORDER_TYPE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 844** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 871** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 52** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 187** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 259** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 277** ! Function 'X_Distance' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 288** ! Function 'X_Distance' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 299** ! Function 'Y_Distance' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 310** ! Function 'Y_Distance' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 321** ! Function 'X_Size' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 332** ! Function 'Y_Size' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 389** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 456** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 474** ! Function 'X_Size' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 485** ! Function 'Y_Size' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 608** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 703** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 760** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 778** ! Function 'X_Size' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 789** ! Function 'Y_Size' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 871** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations

### src/test/resources/samples/opensource/Ilan/Ilan1.6_PipStepExponent_1.4.mq4 (33 issues)

- [ ] **Line 58** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 58** !! Account information sent to external output — potential data privacy risk `AccountInfoExposure`
  - Fix: Avoid logging sensitive account info; use masked output if needed
- [ ] **Line 256** !! OrderSend() without volume validation — validate lot size before sending orders `SafeApiUsage`
  - Fix: Add volume validation: double lot = NormalizeDouble(volume, 2); check against SymbolInfoDouble()
- [ ] **Line 261** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 49** ! Function 'init' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 54** ! Function 'deinit' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 54** ! Public function 'deinit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 58** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 58** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 58** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 58** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 58** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 229** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 241** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 241** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 241** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 241** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 241** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 256** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 256** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 256** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 256** ! Function 'OpenPendingOrder' has 11 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 340** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 351** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 351** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 351** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 391** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 391** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 411** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 411** ! Public function 'FindLastSellPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 411** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)

### src/test/resources/samples/opensource/Ilan/Ilan_HiLo_RSI.mq4 (32 issues)

- [ ] **Line 48** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 48** !! Account information sent to external output — potential data privacy risk `AccountInfoExposure`
  - Fix: Avoid logging sensitive account info; use masked output if needed
- [ ] **Line 248** !! OrderSend() without volume validation — validate lot size before sending orders `SafeApiUsage`
  - Fix: Add volume validation: double lot = NormalizeDouble(volume, 2); check against SymbolInfoDouble()
- [ ] **Line 253** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 39** ! Function 'init' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 44** ! Function 'deinit' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 44** ! Public function 'deinit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 48** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 48** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 48** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 48** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 48** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 220** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 233** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 233** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 233** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 233** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 233** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 248** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 248** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 248** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 248** ! Function 'OpenPendingOrder' has 11 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 335** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 348** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 348** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 348** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 390** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 390** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 412** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 412** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)

### src/test/resources/samples/Include/Arrays/ArrayDouble.mqh (31 issues)

- [ ] **Line 90** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 126** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 148** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 172** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 188** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 218** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 238** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 276** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 322** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 351** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 362** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 377** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 446** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 463** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 463** !! Function 'QuickSort()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 513** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 542** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 557** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 585** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 603** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 622** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 641** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 657** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 672** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 694** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 716** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 716** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 735** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 735** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 463** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 463** ! Parameter 'mode' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/Include/Arrays/ArrayChar.mqh (31 issues)

- [ ] **Line 89** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 125** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 147** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 171** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 187** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 217** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 237** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 275** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 321** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 350** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 361** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 376** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 445** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 462** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 462** !! Function 'QuickSort()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 513** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 542** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 557** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 584** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 601** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 619** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 637** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 653** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 668** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 689** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 710** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 710** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 729** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 729** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 462** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 462** ! Parameter 'mode' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/Include/Arrays/ArrayString.mqh (31 issues)

- [ ] **Line 85** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 121** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 143** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 167** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 183** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 213** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 233** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 271** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 317** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 346** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 357** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 372** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 441** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 458** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 458** !! Function 'QuickSort()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 509** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 538** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 553** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 580** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 597** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 615** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 633** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 649** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 664** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 685** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 706** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 706** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 732** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 732** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 458** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 458** ! Parameter 'mode' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/Include/Arrays/ArrayFloat.mqh (31 issues)

- [ ] **Line 92** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 128** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 150** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 174** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 190** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 220** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 240** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 278** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 324** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 353** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 364** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 379** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 448** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 464** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 464** !! Function 'QuickSort()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 514** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 543** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 558** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 586** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 604** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 623** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 642** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 658** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 673** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 695** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 717** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 717** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 736** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 736** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 464** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 464** ! Parameter 'mode' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/Include/Arrays/ArrayInt.mqh (31 issues)

- [ ] **Line 89** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 125** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 147** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 171** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 187** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 217** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 237** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 275** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 321** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 350** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 361** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 376** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 445** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 462** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 462** !! Function 'QuickSort()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 512** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 541** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 556** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 583** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 600** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 618** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 636** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 652** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 667** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 688** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 709** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 709** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 728** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 728** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 462** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 462** ! Parameter 'mode' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/Include/Arrays/ArrayLong.mqh (31 issues)

- [ ] **Line 89** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 125** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 147** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 171** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 187** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 217** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 237** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 275** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 321** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 350** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 361** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 376** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 445** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 462** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 462** !! Function 'QuickSort()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 512** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 541** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 556** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 583** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 600** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 618** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 636** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 652** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 667** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 688** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 709** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 709** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 728** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 728** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 462** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 462** ! Parameter 'mode' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/Include/Arrays/ArrayShort.mqh (31 issues)

- [ ] **Line 89** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 125** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 147** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 171** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 187** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 217** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 237** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 275** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 321** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 350** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 361** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 376** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 445** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 462** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 462** !! Function 'QuickSort()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 512** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 541** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 556** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 583** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 600** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 618** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 636** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 652** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 667** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 688** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 709** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 709** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 728** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 728** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 462** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 462** ! Parameter 'mode' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/opensource/mql4-lib/Lang/Array.mqh (29 issues)

- [ ] **Line 15** !! User-defined type 'T' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 15** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 19** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 30** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 39** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 45** !! User-defined type 'T' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 62** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 75** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 81** !! User-defined type 'T' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 81** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 94** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 100** !! User-defined type 'T' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 201** !! User-defined type 'T' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 15** ! Parameter 'array' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 15** ! Public function 'ArrayInsert()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 30** ! Parameter 'array' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 30** ! Public function 'ArrayDelete()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 45** ! Public function 'ArrayFind()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 81** ! Parameter 'array' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 81** ! Public function 'ArrayCompact()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 81** ! Parameter 'NULL' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 100** ! Public function 'BinarySearch()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 129** ! Parameter 'result' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 129** ! Public function 'ArrayFindMatch()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 148** ! Class 'Array' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 189** ! Function 'clearArray' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 189** ! Public function 'clearArray()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 201** ! Function 'index' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 201** ! Public function 'index()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/opensource/mql4-lib/Charts/Renko.mqh (28 issues)

- [ ] **Line 56** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 58** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 59** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 60** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 61** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 62** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 67** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 86** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 104** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 12** ! Class 'Renko' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 56** ! Function 'resize' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 56** ! Multiple incremental ArrayResize() calls — use reserve parameter for pre-allocation `MissingArrayPreallocation`
  - Fix: Pre-allocate array: ArrayResize(arr, expectedSize, reserveSize)
- [ ] **Line 67** ! Function 'makeNewBars' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 86** ! Function 'newBar' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 104** ! Function 'move' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 149** ! Parameter 'r' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 149** ! Function 'moveByRate' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 170** ! Parameter 'rs' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 170** ! Function 'updateByRates' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 185** ! Function 'update' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 193** ! Function 'onNewBar' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 193** ! Parameter 'total' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 193** ! Parameter 'bars' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 193** ! Parameter 'open' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 193** ! Parameter 'high' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 194** ! Parameter 'low' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 194** ! Parameter 'close' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 194** ! Parameter 'volume' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/opensource/Ilan/Ilan1.6Dynamic_m.mq4 (26 issues)

- [ ] **Line 59** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 59** !! Account information sent to external output — potential data privacy risk `AccountInfoExposure`
  - Fix: Avoid logging sensitive account info; use masked output if needed
- [ ] **Line 151** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 50** ! Function 'init' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 55** ! Function 'deinit' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 55** ! Public function 'deinit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 59** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 59** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 59** ! Function 'start' is 316 lines long — consider decomposing into smaller functions `LongFunction`
  - Fix: Split long function into smaller, focused functions
- [ ] **Line 377** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 401** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 401** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 401** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 401** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 401** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 500** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 615** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 667** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 667** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 687** ! OrderSelect() result ignored — check its bool return before reading order data `OrderSelectUnchecked`
- [ ] **Line 687** ! Public function 'FindLastSellPrice()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 687** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 707** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 726** ! Public function 'ModifyOrder()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/opensource/mql4-lib/OpenCL/OpenCL.mqh (25 issues)

- [ ] **Line 884** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 892** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 915** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 924** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 927** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 980** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1040** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 1043** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 1050** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 1053** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 902** ! Function 'getInfo' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 915** ! Function 'getDeviceCount' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 924** ! Parameter 'devices' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 924** ! Function 'getDevices' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 961** ! Parameter 'platform' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 980** ! Parameter 'platform' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 980** ! Function 'initContext' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 996** ! Parameter 'platform' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 1013** ! Function 'getInfoSizeInBytes' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 1022** ! Function 'getReferenceCount' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 1031** ! Function 'getDeviceCount' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 1040** ! Parameter 'devices' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 1040** ! Function 'getDevices' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 1050** ! Parameter 'properties' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 1050** ! Function 'getProperties' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/Include/ChartObjects/ChartObjectsBmpControls.mqh (23 issues)

- [ ] **Line 129** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 152** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 318** !! User-defined type 'ENUM_BASE_CORNER' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 439** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 481** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 49** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 85** ! Function 'X_Offset' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 96** ! Function 'X_Offset' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 107** ! Function 'Y_Offset' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 118** ! Function 'Y_Offset' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 152** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 225** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 241** ! Function 'X_Distance' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 252** ! Function 'X_Distance' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 263** ! Function 'Y_Distance' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 274** ! Function 'Y_Distance' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 285** ! Function 'X_Size' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 296** ! Function 'Y_Size' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 395** ! Function 'X_Offset' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 406** ! Function 'X_Offset' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 417** ! Function 'Y_Offset' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 428** ! Function 'Y_Offset' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 481** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations

### src/test/resources/samples/Include/Charts/Chart.mqh (23 issues)

- [ ] **Line 176** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 197** !! User-defined type 'ENUM_CHART_MODE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 601** !! User-defined type 'ENUM_CHART_VOLUME_MODE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1140** !! User-defined type 'ENUM_CHART_POSITION' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1151** !! User-defined type 'ENUM_CHART_PROPERTY_INTEGER' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1162** !! User-defined type 'ENUM_CHART_PROPERTY_INTEGER' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1173** !! User-defined type 'ENUM_CHART_PROPERTY_INTEGER' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1184** !! User-defined type 'ENUM_CHART_PROPERTY_DOUBLE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1195** !! User-defined type 'ENUM_CHART_PROPERTY_DOUBLE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1206** !! User-defined type 'ENUM_CHART_PROPERTY_DOUBLE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1217** !! User-defined type 'ENUM_CHART_PROPERTY_STRING' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1228** !! User-defined type 'ENUM_CHART_PROPERTY_STRING' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1239** !! User-defined type 'ENUM_CHART_PROPERTY_STRING' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1250** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1272** !! User-defined type 'ENUM_ALIGN_MODE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 9** ! Public function 'ChartIndicatorAdd()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 9** ! Parameter 'chart_id' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 9** ! Parameter 'subwin' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 9** ! Parameter 'handle' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 176** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 348** ! Function 'ScaleFix_11' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 359** ! Function 'ScaleFix_11' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 1471** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations

### src/test/resources/samples/Include/Controls/WndContainer.mqh (22 issues)

- [ ] **Line 61** !! Function 'OnEvent()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 83** !! Function 'OnMouseEvent()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 119** !! Function 'Destroy()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 136** !! Function 'ControlFind()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 160** !! Function 'MouseFocusKill()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 181** !! Function 'Add()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 207** !! Function 'Add()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 215** !! Function 'Delete()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 238** !! Function 'Delete()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 262** !! Function 'Shift()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 283** !! Function 'Id()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 304** !! Function 'Enable()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 322** !! Function 'Disable()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 340** !! Function 'Show()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 358** !! Function 'Hide()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 437** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 456** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 119** ! Parameter 'reason' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 136** ! Public function 'ControlFind()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 160** ! Parameter 'id' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 207** ! Parameter 'control' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 238** ! Parameter 'control' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param

### src/test/resources/samples/opensource/mql4-lib/Trade/OrderManager.mqh (22 issues)

- [ ] **Line 13** !! Class 'OrderManager' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 114** !! OrderSend() without volume validation — validate lot size before sending orders `SafeApiUsage`
  - Fix: Add volume validation: double lot = NormalizeDouble(volume, 2); check against SymbolInfoDouble()
- [ ] **Line 174** !! Function 'modify()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 13** ! Class 'OrderManager' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 76** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 87** ! Function 'getOrderType' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 114** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 114** ! Function 'send' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 114** ! OrderSend() failure without retry or recovery logic `MissingErrorRecovery`
  - Fix: Add retry logic with Sleep() and attempt counter for failed OrderSend()
- [ ] **Line 130** ! Function 'buy' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 137** ! Function 'sell' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 144** ! Function 'pendBuy' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 153** ! Function 'pendSell' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 162** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 162** ! Function 'modify' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 174** ! Function 'modify' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 195** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 195** ! Function 'modifyPending' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 207** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 207** ! Function 'closeCurrent' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 239** ! Function 'close' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 247** ! Function 'closeBy' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/Include/Files/FileBin.mqh (22 issues)

- [ ] **Line 79** !! Function 'Open()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 247** ! Parameter 'array' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 247** ! Public function 'WriteArray()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 247** ! Parameter 'WHOLE_ARRAY' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 259** ! Parameter 'data' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 259** ! Public function 'WriteStruct()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 391** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 405** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 416** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 427** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 438** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 449** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 460** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 472** ! Parameter 'array' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 472** ! Public function 'ReadArray()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 472** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 472** ! Parameter 'WHOLE_ARRAY' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 484** ! Parameter 'data' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 484** ! Public function 'ReadStruct()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 484** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 508** ! Parameter 'value' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 508** ! Public function 'ReadEnum()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/opensource/mql4-lib/Collection/LinkedList.mqh (20 issues)

- [ ] **Line 41** !! Class 'LinkedList' uses new/delete but lacks copy constructor or assignment operator (Rule of Three) `IncompleteClass`
  - Fix: Implement copy constructor and assignment operator, or delete them
- [ ] **Line 178** !! User-defined type 'T' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 218** !! Class 'ListIterator' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 15** ! Class 'LinkedNode' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 41** ! Class 'LinkedList' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 109** ! Function 'detach' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 109** ! Public function 'detach()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 122** ! Function 'insert' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 122** ! Public function 'insert()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 138** ! Function 'last' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 138** ! Public function 'last()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 150** ! Function 'at' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 150** ! Public function 'at()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 161** ! Function 'clear' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 161** ! Public function 'clear()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 178** ! Function 'remove' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 178** ! Public function 'remove()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 201** ! Function 'compact' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 201** ! Public function 'compact()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 218** ! Class 'ListIterator' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/opensource/mql4-lib/Collection/Collection.mqh (20 issues)

- [ ] **Line 70** !! User-defined type 'T' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 111** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 111** !! 'delete' used without prior NULL check — risk of use-after-free `DeleteWithoutNullCheck`
  - Fix: Add NULL check before delete: if(ptr != NULL) { delete ptr; ptr = NULL; }
- [ ] **Line 111** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 111** !! 'delete iter' without subsequent 'iter = NULL' — leaves dangling pointer `NullAfterDelete`
  - Fix: Set pointer to NULL after delete: delete ptr; ptr = NULL;
- [ ] **Line 116** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 15** ! Class 'Iterator' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 27** ! Class 'ElementOperator' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 38** ! Class 'Iterable' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 46** ! Class 'Collection' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 70** ! Function 'contains' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 70** ! Public function 'contains()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 82** ! Parameter 'array' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 82** ! Function 'addAll' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 82** ! Public function 'addAll()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 97** ! Function 'addAll' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 97** ! Public function 'addAll()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 111** ! Parameter 'array' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 111** ! Function 'toArray' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 111** ! Public function 'toArray()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/opensource/mql4-lib/Utils/Fuzzy.mqh (20 issues)

- [ ] **Line 71** !! Class 'FuzzySSR' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 244** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 244** !! User-defined type 'NumericFunction' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 266** !! Class 'FuzzyDSR' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 314** !! User-defined type 'NumericFunction' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 314** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 316** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 317** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 323** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 323** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 325** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 326** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 48** ! Class 'FuzzySet' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 71** ! Class 'FuzzySSR' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 223** ! Function 'membership' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 233** ! Function 'alphacut' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 266** ! Class 'FuzzyDSR' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 314** ! Function 'discretize' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 323** ! Function 'discretize' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 344** ! Function 'membership' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/Include/Files/FilePipe.mqh (19 issues)

- [ ] **Line 67** !! Function 'Open()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 92** !! User-defined type 'T' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 92** ! Public function 'WriteInteger()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 165** ! Parameter 'array' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 165** ! Public function 'WriteArray()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 165** ! Parameter 'WHOLE_ARRAY' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 177** ! Parameter 'data' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 177** ! Public function 'WriteStruct()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 201** ! Parameter 'value' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 201** ! Public function 'ReadInteger()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 284** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 299** ! Parameter 'array' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 299** ! Public function 'ReadArray()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 299** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 314** ! Parameter 'data' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 314** ! Public function 'ReadStruct()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 314** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 338** ! Parameter 'value' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 338** ! Public function 'ReadEnum()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/Include/ChartObjects/ChartObject.mqh (18 issues)

- [ ] **Line 114** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 276** !! User-defined type 'ENUM_LINE_STYLE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 585** !! User-defined type 'ENUM_LINE_STYLE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 676** !! User-defined type 'ENUM_OBJECT_PROPERTY_INTEGER' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 690** !! User-defined type 'ENUM_OBJECT_PROPERTY_INTEGER' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 701** !! User-defined type 'ENUM_OBJECT_PROPERTY_INTEGER' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 712** !! User-defined type 'ENUM_OBJECT_PROPERTY_INTEGER' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 723** !! User-defined type 'ENUM_OBJECT_PROPERTY_DOUBLE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 737** !! User-defined type 'ENUM_OBJECT_PROPERTY_DOUBLE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 748** !! User-defined type 'ENUM_OBJECT_PROPERTY_DOUBLE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 759** !! User-defined type 'ENUM_OBJECT_PROPERTY_DOUBLE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 770** !! User-defined type 'ENUM_OBJECT_PROPERTY_STRING' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 784** !! User-defined type 'ENUM_OBJECT_PROPERTY_STRING' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 795** !! User-defined type 'ENUM_OBJECT_PROPERTY_STRING' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 806** !! User-defined type 'ENUM_OBJECT_PROPERTY_STRING' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 353** ! Function 'Z_Order' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 364** ! Function 'Z_Order' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 942** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations

### src/test/resources/samples/opensource/mql4-lib/Charts/RenkoChart.mqh (17 issues)

- [ ] **Line 94** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 13** ! Class 'RenkoChart' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 33** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 33** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 33** ! Parameter 'barSize' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 33** ! Parameter 'std' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 48** ! Function 'getRate' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 64** ! Parameter 'rs' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 64** ! Function 'loadHistory' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 64** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 94** ! Function 'onNewBar' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 94** ! Parameter 'total' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 94** ! Parameter 'open' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 94** ! Parameter 'high' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 94** ! Parameter 'low' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 94** ! Parameter 'close' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 94** ! Parameter 'volume' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/opensource/mql4-lib/Trade/FxSymbol.mqh (17 issues)

- [ ] **Line 23** !! Class 'FxSymbol' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 192** !! Class 'CurrencyIndexEntry' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 235** !! Class 'SymbolAnalyzer' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 263** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 268** !! Heap allocation ('new') inside loop — causes repeated allocation overhead and potential memory leaks `NewKeywordInLoop`
  - Fix: Move 'new' allocation outside loop; reuse objects or use object pool
- [ ] **Line 306** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 23** ! Class 'FxSymbol' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 192** ! Class 'CurrencyIndexEntry' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 235** ! Class 'SymbolAnalyzer' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 263** ! Function 'buildCurrencyIndex' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 306** ! Function 'getConversionSymbol' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 328** ! Function 'convertToAccount' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 346** ! Function 'convertToAccountWithMid' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 364** ! Function 'getLeverage' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 421** ! Function 'getRequiredMargin' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 483** ! Function 'getProfit' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 522** ! Function 'getMaxLots' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/Include/Controls/ListView.mqh (16 issues)

- [ ] **Line 99** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 99** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 99** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 117** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 131** !! Function 'Destroy()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 161** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 161** !! Function 'Show()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 180** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 212** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 250** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 301** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 338** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 412** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 462** !! Function 'OnResize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 478** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 498** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }

### src/test/resources/samples/opensource/mql4-lib/Charts/PivotGroup.mqh (16 issues)

- [ ] **Line 56** !! Function 'draw()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 74** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 74** !! CopyRates/CopyBuffer return value should be checked (returns -1 on failure) `UncheckedCopyRates`
  - Fix: Check CopyRates/CopyBuffer return: if(CopyRates(...) <= 0) { Print("Copy failed"); return; }
- [ ] **Line 74** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 74** !! Function 'draw()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 94** !! Function 'remove()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 13** ! Class 'PivotGroup' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 35** ! Parameter 'pivotIdPrefix' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 35** ! Parameter 'linesIdPrefix' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 35** ! Parameter 'labelPrefix' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 35** ! Parameter 'pColor' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 35** ! Parameter 'rColor' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 35** ! Parameter 'sColor' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 56** ! Function 'draw' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 74** ! Function 'draw' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 94** ! Function 'remove' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/Include/Indicators/Custom.mqh (15 issues)

- [ ] **Line 59** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 70** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 74** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 87** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 105** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 115** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 115** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 115** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 120** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 12** ! Class 'CiCustom' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 52** ! Parameter 'buffers' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 115** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 115** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 135** ! Parameter 'buffer_num' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 135** ! Parameter 'index' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/Include/Arrays/List.mqh (15 issues)

- [ ] **Line 92** !! Function 'QuickSort()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 404** !! 'delete result' without subsequent 'result = NULL' — leaves dangling pointer `NullAfterDelete`
  - Fix: Set pointer to NULL after delete: delete ptr; ptr = NULL;
- [ ] **Line 599** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 628** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 92** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 226** ! Public function 'GetNodeAtIndex()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 303** ! Public function 'GetFirstNode()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 316** ! Public function 'GetPrevNode()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 329** ! Public function 'GetCurrentNode()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 336** ! Public function 'GetNextNode()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 349** ! Public function 'GetLastNode()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 362** ! Public function 'DetachCurrent()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 554** ! Public function 'QuickSearch()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 585** ! Public function 'Search()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 628** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations

### src/test/resources/samples/opensource/mql4-lib/Utils/ChartFile.mqh (15 issues)

- [ ] **Line 97** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 10** ! Class 'ChartFile' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 69** ! Function 'openChart' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 79** ! Function 'closeChart' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 90** ! Function 'isClosed' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 97** ! Function 'writeHeader' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 120** ! Function 'skipHeader' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 130** ! Function 'flush' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 140** ! Function 'hardGetNumberOfRecords' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 163** ! Function 'gotoRecord' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 173** ! Parameter 'r' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 173** ! Function 'readRecord' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 173** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 183** ! Function 'writeRecord' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 194** ! Function 'updateRecord' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/Include/Controls/CheckGroup.mqh (14 issues)

- [ ] **Line 90** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 90** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 106** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 116** !! Function 'Destroy()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 128** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 149** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 201** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 212** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 233** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 233** !! Function 'Show()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 275** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 304** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 324** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 361** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }

### src/test/resources/samples/Include/Controls/DateDropList.mqh (14 issues)

- [ ] **Line 96** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 165** !! Function 'Show()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 177** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 197** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 230** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 239** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 284** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 374** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 385** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 399** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 96** ! Parameter 'x2' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 96** ! Parameter 'y2' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 230** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 284** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions

### src/test/resources/samples/Include/Controls/Dialog.mqh (14 issues)

- [ ] **Line 111** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 135** !! Function 'Add()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 142** !! Function 'Add()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 149** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 165** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 630** !! Function 'Destroy()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 919** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 947** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 959** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 142** ! Parameter 'control' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 850** ! Parameter 'dparam' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 850** ! Parameter 'sparam' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 919** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 919** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions

### src/test/resources/samples/Indicators/iExposure.mq4 (13 issues)

- [ ] **Line 45** !! OnInit() should return 'int' (not 'void') to report initialization status `MissingOnInitReturn`
  - Fix: Change return type to 'int'
- [ ] **Line 65** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 178** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 217** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 65** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 65** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 84** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 102** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 110** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 150** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 178** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)

### src/test/resources/samples/opensource/mql4-lib/Charts/LabeledLine.mqh (13 issues)

- [ ] **Line 48** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 59** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 72** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 73** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 10** ! Class 'LabeledLine' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 30** ! Parameter 'lineId' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 30** ! Parameter 'labelId' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 30** ! Parameter 'labelName' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 30** ! Parameter 'lineStyle' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 30** ! Parameter 'lineColor' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 44** ! Function 'draw' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 44** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 70** ! Function 'remove' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/opensource/mql4-lib/UI/FreeFormElement.mqh (13 issues)

- [ ] **Line 51** !! Class 'Panel' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 73** !! Class 'Label' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 87** !! Class 'Bitmap' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 99** !! Class 'Edit' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 114** !! Class 'Button' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 17** ! Class 'FreeFormElement' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 51** ! Class 'Panel' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 62** ! Parameter 'parent' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 62** ! Parameter 'name' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 73** ! Class 'Label' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 87** ! Class 'Bitmap' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 99** ! Class 'Edit' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 114** ! Class 'Button' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/opensource/mql4-lib/Charts/FibGroup.mqh (13 issues)

- [ ] **Line 54** !! Function 'draw()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 69** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 69** !! CopyRates/CopyBuffer return value should be checked (returns -1 on failure) `UncheckedCopyRates`
  - Fix: Check CopyRates/CopyBuffer return: if(CopyRates(...) <= 0) { Print("Copy failed"); return; }
- [ ] **Line 69** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 69** !! Function 'draw()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 89** !! Function 'remove()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 13** ! Class 'FibGroup' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 35** ! Parameter 'idPrefix' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 35** ! Parameter 'labelPrefix' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 35** ! Parameter 'fColor' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 54** ! Function 'draw' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 69** ! Function 'draw' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 89** ! Function 'remove' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/Include/ChartObjects/ChartObjectsArrows.mqh (13 issues)

- [ ] **Line 95** !! User-defined type 'ENUM_ARROW_ANCHOR' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 106** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 126** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 48** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 126** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 176** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 221** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 266** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 311** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 356** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 401** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 447** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 492** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details

### src/test/resources/samples/opensource/mql4-lib/Charts/PriceBreak.mqh (13 issues)

- [ ] **Line 13** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 19** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 26** !! User-defined type 'ENUM_APPLIED_PRICE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 100** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 115** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 142** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 181** !! User-defined type 'ENUM_APPLIED_PRICE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 13** ! Parameter 'a' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 13** ! Public function 'GrowBuffer()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 51** ! Class 'PriceBreak' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 92** ! Parameter 'distance' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 181** ! Parameter 'r' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 181** ! Parameter 'PRICE_CLOSE' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/Include/ChartObjects/ChartObjectsFibo.mqh (12 issues)

- [ ] **Line 246** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 266** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 41** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 86** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 131** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 184** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 184** ! Function 'Create' has 8 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 266** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 316** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 316** ! Function 'Create' has 9 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 363** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 363** ! Function 'Create' has 9 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/Include/ChartObjects/ChartObjectsGann.mqh (12 issues)

- [ ] **Line 87** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 104** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 220** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 240** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 359** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 379** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 47** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 104** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 158** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 240** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 297** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 379** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations

### src/test/resources/samples/Experts/Moving Average.mq4 (12 issues)

- [ ] **Line 73** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 73** !! OrderSend() without volume validation — validate lot size before sending orders `SafeApiUsage`
  - Fix: Add volume validation: double lot = NormalizeDouble(volume, 2); check against SymbolInfoDouble()
- [ ] **Line 98** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 98** !! Closing/deleting orders in a forward OrdersTotal() loop skips orders — iterate downward (for(i=OrdersTotal()-1; i>=0; i--)) `OrderCloseLoopDirection`
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 20** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 73** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 73** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 98** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 98** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 98** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 98** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions

### src/test/resources/samples/Include/ChartObjects/ChartObjectPanel.mqh (12 issues)

- [ ] **Line 73** !! Function 'X_Distance()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 88** !! Function 'Y_Distance()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 103** !! Function 'X_Size()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 122** !! Function 'Y_Size()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 141** !! Function 'Timeframes()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 159** !! Function 'State()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 73** ! Function 'X_Distance' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 88** ! Function 'Y_Distance' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 103** ! Function 'X_Size' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 103** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 122** ! Function 'Y_Size' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 122** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions

### src/test/resources/samples/Include/Controls/Wnd.mqh (12 issues)

- [ ] **Line 317** !! Function 'Width()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 327** !! Function 'Height()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 337** !! Function 'Size()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 347** !! Function 'Size()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 357** !! Function 'Move()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 367** !! Function 'Move()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 377** !! Function 'Shift()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 387** !! Function 'Contains()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 640** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 666** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 296** ! Parameter 'reason' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 305** ! Public function 'ControlFind()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/Include/Controls/RadioGroup.mqh (11 issues)

- [ ] **Line 87** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 87** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 103** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 113** !! Function 'Destroy()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 124** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 145** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 211** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 211** !! Function 'Show()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 282** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 311** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 331** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }

### src/test/resources/parser/templates/TemplateFunctions.mq4 (11 issues)

- [ ] **Line 2** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 7** !! User-defined type 'T2' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 2** ! Parameter 'arr' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 2** ! Public function 'ArrayMax()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 7** ! Parameter 'var1' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 7** ! Public function 'Assign()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 12** ! Function 'foo' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 12** ! Public function 'foo()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 12** ! Parameter 'con' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/opensource/mql4-lib/Lang/Pointer.mqh (11 issues)

- [ ] **Line 34** !! 'delete' used without prior NULL check — risk of use-after-free `DeleteWithoutNullCheck`
  - Fix: Add NULL check before delete: if(ptr != NULL) { delete ptr; ptr = NULL; }
- [ ] **Line 34** !! 'delete pointer' without subsequent 'pointer = NULL' — leaves dangling pointer `NullAfterDelete`
  - Fix: Set pointer to NULL after delete: delete ptr; ptr = NULL;
- [ ] **Line 43** !! User-defined type 'T' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 13** ! Public function 'IsValid()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 21** ! Public function 'IsPointer()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 34** ! Public function 'SafeDelete()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 43** ! Public function 'SafeDelete()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 43** ! Parameter 'pointer' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 48** ! Class 'Ref' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 68** ! Class 'Ptr' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 92** ! Public function 'GetAddress()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/Include/Controls/TimePicker.mqh (10 issues)

- [ ] **Line 104** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 187** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 187** !! User-defined type 'ENUM_TIME_MODES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 198** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 232** !! Function 'Show()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 303** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 311** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 333** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 198** ! Possible unintended switch case fall-through — missing 'break' or 'return' statement `MissingBreakInSwitch`
  - Fix: Add 'break;' at end of case, or add '// fallthrough' comment if intentional
- [ ] **Line 341** ! Possible unintended switch case fall-through — missing 'break' or 'return' statement `MissingBreakInSwitch`
  - Fix: Add 'break;' at end of case, or add '// fallthrough' comment if intentional

### src/test/resources/parser/classes/Methods.mqh (10 issues)

- [ ] **Line 30** !! Class 'D' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 3** ! Class 'A' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 9** ! Class 'B' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 16** ! Function 'm' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 16** ! Public function 'm()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 19** ! Class 'C' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 30** ! Class 'D' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 36** ! Class 'E' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 40** ! Function 'x' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 40** ! Public function 'x()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/opensource/mql4-lib/Lang/GlobalVariable.mqh (10 issues)

- [ ] **Line 57** !! Class 'AtomicVar' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 85** !! Class 'Semaphore' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 10** ! Class 'GlobalVariable' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 32** ! Class 'TempVar' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 57** ! Class 'AtomicVar' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 70** ! Function 'increment' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 85** ! Class 'Semaphore' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 98** ! Parameter 'name' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 109** ! Function 'acquire' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 118** ! Function 'release' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/opensource/mql4-lib/Collection/Set.mqh (10 issues)

- [ ] **Line 14** !! Class 'Set' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 35** !! User-defined type 'T' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 50** !! User-defined type 'T' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 65** !! Class 'SetIterator' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 14** ! Class 'Set' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 35** ! Function 'add' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 35** ! Public function 'add()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 50** ! Function 'remove' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 50** ! Public function 'remove()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 65** ! Class 'SetIterator' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/Include/Indicators/TimeSeries.mqh (9 issues)

- [ ] **Line 44** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 306** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 389** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 73** ! Class 'CiOpen' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 123** ! Class 'CiHigh' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 173** ! Class 'CiLow' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 223** ! Class 'CiClose' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 273** ! Class 'CiTime' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 356** ! Class 'CiVolume' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/Include/ChartObjects/ChartObjectsChannels.mqh (9 issues)

- [ ] **Line 133** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 150** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 42** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 42** ! Function 'Create' has 9 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 94** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 150** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 195** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 240** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 240** ! Function 'Create' has 9 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/mql5/MQL5_Sample_EA.mq5 (9 issues)

- [ ] **Line 35** !! IndicatorRelease() called multiple times — potential double-free `DoubleIndicatorRelease`
  - Fix: Remove duplicate IndicatorRelease() call
- [ ] **Line 44** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 44** !! Print()/Comment() in OnTick() causes excessive logging and performance degradation `PrintInOnTick`
  - Fix: Guard with condition: if(enableLogging) Print(...); or use Comment() sparingly
- [ ] **Line 115** !! Class 'CMyStrategy' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 44** ! Constant lookups repeated in OnTick() — consider caching in a variable `RedundantCalculationInOnTick`
  - Fix: Cache calculation result in a static/global variable, recalculate only on new bar
- [ ] **Line 44** ! Indicator value computed before conditional check — consider lazy evaluation `LazyEvaluationMiss`
  - Fix: Reorder conditions to check cheap/likely-false conditions first
- [ ] **Line 44** ! OrderSend() failure without retry or recovery logic `MissingErrorRecovery`
  - Fix: Add retry logic with Sleep() and attempt counter for failed OrderSend()
- [ ] **Line 44** ! Deprecated MQL4 function 'OrderSend()' used — consider MQL5 equivalent 'CTrade.OrderSend()' `ModernMQL5Idiom`
  - Fix: Replace deprecated MQL4 function with MQL5 equivalent
- [ ] **Line 87** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)

### src/test/resources/samples/Experts/MACD Sample.mq4 (9 issues)

- [ ] **Line 18** !! Print()/Comment() in OnTick() causes excessive logging and performance degradation `PrintInOnTick`
  - Fix: Guard with condition: if(enableLogging) Print(...); or use Comment() sparingly
- [ ] **Line 18** !! OrderSend() without volume validation — validate lot size before sending orders `SafeApiUsage`
  - Fix: Add volume validation: double lot = NormalizeDouble(volume, 2); check against SymbolInfoDouble()
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 18** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 18** ! Prices (Bid/Ask) read in a loop without RefreshRates() — values may be stale `MissingRefreshRates`
- [ ] **Line 18** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 18** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 18** ! Excessive nesting depth (8 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions

### src/test/resources/samples/opensource/mql4-lib/Trade/OrderGroup.mqh (9 issues)

- [ ] **Line 46** !! User-defined type 'OrderDoubleProperty' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 21** ! Class 'OrderGroup' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 46** ! Function 'groupDoubleProperty' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 60** ! Function 'groupAvg' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 77** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 77** ! Function 'groupTakeProfit' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 91** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 91** ! Function 'groupStopLoss' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 105** ! Function 'clearClosed' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/opensource/mql4-lib/Utils/ParseUtils.mqh (8 issues)

- [ ] **Line 10** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 23** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 39** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }
- [ ] **Line 52** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 10** ! Function 'parseToPositiveIntegers' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 24** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 39** ! Function 'parseToPositiveDoubles' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 53** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops

### src/test/resources/samples/Indicators/Cayman-AMarkets.mq4 (8 issues)

- [ ] **Line 29** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 22** ! Public function 'OnInit()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 29** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 29** ! Public function 'start()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 68** ! Public function 'IntPutInRange()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 78** ! Public function 'FindCaymanSymbol()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 104** ! Public function 'CheckCaymanSymbol()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/Include/Files/File.mqh (8 issues)

- [ ] **Line 100** !! FileOpen() without corresponding FileClose() — potential resource leak `MissingFileClose`
  - Fix: Add FileClose(handle) after file operations
- [ ] **Line 176** !! User-defined type 'ENUM_FILE_POSITION' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 253** !! Function 'FolderCreate()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 260** !! Function 'FolderDelete()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 267** !! Function 'FolderClean()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 274** !! Function 'FileFindFirst()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 281** !! Function 'FileFindNext()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 288** !! Function 'FileFindClose()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach

### src/test/resources/samples/Include/Indicators/Indicators.mqh (8 issues)

- [ ] **Line 52** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 52** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 53** !! User-defined type 'ENUM_INDICATOR' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 57** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 262** !! Function 'BufferResize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 280** !! Function 'Refresh()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 52** ! Public function 'Create()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 52** ! Function 'Create' is 205 lines long — consider decomposing into smaller functions `LongFunction`
  - Fix: Split long function into smaller, focused functions

### src/test/resources/parser/mql5/MQL5Keywords.mq4 (8 issues)

- [ ] **Line 51** !! User-defined type 'T' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 51** !! User-defined type 'T' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 33** ! Class 'ISerializable' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 46** ! Public function 'ProcessStrategy()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 46** ! Parameter 'strategy' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 51** ! Public function 'MaxValue()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/Include/MovingAverages.mqh (8 issues)

- [ ] **Line 11** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 28** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 44** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 65** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 86** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 123** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 159** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 206** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }

### src/test/resources/samples/Include/Indicators/Indicator.mqh (8 issues)

- [ ] **Line 96** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 97** !! User-defined type 'ENUM_INDICATOR' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 107** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 127** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 151** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 97** ! Parameter 'type' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 97** ! Parameter 'num_params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 97** ! Parameter 'params' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/Include/ChartObjects/ChartObjectsLines.mqh (8 issues)

- [ ] **Line 193** !! Function 'Save()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 213** !! Function 'Load()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 39** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 80** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 133** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 213** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 265** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 332** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details

### src/test/resources/samples/opensource/mql4-lib/Collection/Vector.mqh (8 issues)

- [ ] **Line 14** !! Class 'Vector' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 48** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 48** !! User-defined type 'T' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 64** !! Class 'VectorIterator' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 14** ! Class 'Vector' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 48** ! Function 'remove' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 48** ! Public function 'remove()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 64** ! Class 'VectorIterator' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/Indicators/Examples/PanelDialog.mqh (8 issues)

- [ ] **Line 99** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 281** !! Function 'OnResize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 224** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 248** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 272** ! String concatenation inside loop — use StringConcatenate() or StringAdd() for better performance `StringConcatInLoop`
  - Fix: Use StringConcatenate() or StringAdd() instead of '+' in loops
- [ ] **Line 351** ! Parameter 'lparam' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 351** ! Parameter 'dparam' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 351** ! Parameter 'sparam' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/Include/Controls/Edit.mqh (7 issues)

- [ ] **Line 59** !! Function 'OnEvent()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 85** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 99** !! Function 'ReadOnly()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 109** !! User-defined type 'ENUM_ALIGN_MODE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 109** !! Function 'TextAlign()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 157** !! Function 'OnChange()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 175** !! Function 'OnClick()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach

### src/test/resources/samples/Indicators/Custom Moving Averages.mq4 (7 issues)

- [ ] **Line 74** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 87** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 112** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 134** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 170** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 53** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/parser/preprocessor/Property.mq4 (7 issues)

- [ ] **Line 4** !! Unknown property `PreprocessorProperty`
  - Fix: Review #property directive syntax and values
- [ ] **Line 7** !! Argument is expected `PreprocessorProperty`
  - Fix: Review #property directive syntax and values
- [ ] **Line 8** !! Argument is expected `PreprocessorProperty`
  - Fix: Review #property directive syntax and values
- [ ] **Line 17** !! Argument is expected `PreprocessorProperty`
  - Fix: Review #property directive syntax and values
- [ ] **Line 22** !! Unknown property `PreprocessorProperty`
  - Fix: Review #property directive syntax and values
- [ ] **Line 30** !! Unknown property `PreprocessorProperty`
  - Fix: Review #property directive syntax and values
- [ ] **Line 30** !! Unknown property `PreprocessorProperty`
  - Fix: Review #property directive syntax and values

### src/test/resources/samples/Include/Tools/DateTime.mqh (7 issues)

- [ ] **Line 59** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 82** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 105** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 123** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 143** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 170** !! Function 'Date()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 192** !! Function 'Time()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach

### src/test/resources/samples/opensource/mql4-lib/Trade/Order.mqh (6 issues)

- [ ] **Line 23** !! Class 'Order' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 138** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 12** ! Class 'OrderMatcher' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 23** ! Class 'Order' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 138** ! Function 'toString' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 172** ! Function 'hash' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/opensource/mql4-lib/Trade/IndicatorDriver.mqh (6 issues)

- [ ] **Line 73** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 13** ! Class 'IndicatorDriver' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 38** ! Function 'add' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 56** ! Function 'remove' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 73** ! Function 'calculate' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 73** ! Function 'calculate' has 9 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/opensource/mql4-lib/UI/Chart.mqh (6 issues)

- [ ] **Line 10** !! Class 'Chart' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 65** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 10** ! Class 'Chart' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 59** ! Parameter 'chartId' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 65** ! Parameter 'symbol' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 65** ! Parameter 'period' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/opensource/mql4-lib/UI/ActionMarker.mqh (6 issues)

- [ ] **Line 48** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 12** ! Class 'ActionEventHandler' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 21** ! Class 'ActionMarker' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 45** ! Function 'ensureCreated' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 45** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 66** ! Function 'check' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/Include/Controls/ComboBox.mqh (6 issues)

- [ ] **Line 104** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 180** !! Function 'AddItem()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 188** !! Function 'Select()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 198** !! Function 'SelectByText()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 208** !! Function 'SelectByValue()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 218** !! Function 'Show()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach

### src/test/resources/samples/Include/Arrays/TreeNode.mqh (6 issues)

- [ ] **Line 63** !! 'delete m_l_node' without subsequent 'm_l_node = NULL' — leaves dangling pointer `NullAfterDelete`
  - Fix: Set pointer to NULL after delete: delete ptr; ptr = NULL;
- [ ] **Line 74** !! Function 'RefreshBalance()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 107** !! Function 'SaveNode()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 137** !! Function 'LoadNode()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 97** ! Public function 'GetNext()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 137** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations

### src/test/resources/samples/Indicators/ZigZag.mq4 (6 issues)

- [ ] **Line 50** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 185** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 50** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 50** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/parser/statements/ControlFlow.mq4 (6 issues)

- [ ] **Line 5** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 5** !! Print()/Comment() in OnTick() causes excessive logging and performance degradation `PrintInOnTick`
  - Fix: Guard with condition: if(enableLogging) Print(...); or use Comment() sparingly
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 76** ! Public function 'Add()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 82** ! Public function 'Ratio()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/opensource/mql4-lib/Lang/App.mqh (5 issues)

- [ ] **Line 71** !! Class 'AppParamSetter' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 97** !! Class 'App' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 63** ! Class 'AppParam' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 71** ! Class 'AppParamSetter' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 97** ! Class 'App' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/Scripts/DLLSampleTester.mq4 (5 issues)

- [ ] **Line 47** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 27** ! Function 'init' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 47** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/Indicators/Parabolic.mq4 (5 issues)

- [ ] **Line 30** !! OnInit() should return 'int' (not 'void') to report initialization status `MissingOnInitReturn`
  - Fix: Change return type to 'int'
- [ ] **Line 67** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 67** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 67** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/Include/Arrays/Tree.mqh (5 issues)

- [ ] **Line 92** !! 'delete node' without subsequent 'node = NULL' — leaves dangling pointer `NullAfterDelete`
  - Fix: Set pointer to NULL after delete: delete ptr; ptr = NULL;
- [ ] **Line 58** ! Public function 'Insert()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 106** ! Excessive nesting depth (5 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions
- [ ] **Line 233** ! Public function 'Find()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 245** ! Excessive nesting depth (7 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions

### src/test/resources/samples/Indicators/Bands.mq4 (5 issues)

- [ ] **Line 68** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 123** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 68** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 123** ! Function 'StdDev_Func' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/Indicators/userindicator.mq4 (5 issues)

- [ ] **Line 21** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 12** ! Function 'init' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 21** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/Scripts/PeriodConverter.mq4 (5 issues)

- [ ] **Line 17** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 167** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 168** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 17** ! Excessive nesting depth (6 levels) — consider extracting methods to reduce complexity `DeepNesting`
  - Fix: Extract deeply nested code into separate functions

### src/test/resources/samples/opensource/mql4-lib/UI/ReadOnlyLabel.mqh (5 issues)

- [ ] **Line 32** !! Important function return value ignored — check return values of ArrayResize/FileOpen/ObjectCreate/etc. to detect failures `ReturnValueIgnored`
  - Fix: Check return value: if(!FunctionCall()) { Print("Error"); }
- [ ] **Line 9** ! Class 'ReadOnlyLabel' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 29** ! Function 'ensureCreated' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 29** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 50** ! Function 'render' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/opensource/mql4-lib/Lang/Hash.mqh (5 issues)

- [ ] **Line 196** !! Function 'Hash()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 210** !! Function 'Hash()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 15** ! Function 'MurmurHash3_x86_32' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 63** ! Function 'MurmurHash3_x86_32_String' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 225** ! Public function 'Hash()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/opensource/mql4-lib/UI/UIElement.mqh (5 issues)

- [ ] **Line 13** !! Class 'UIElement' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 44** !! Function 'findByName()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 13** ! Class 'UIElement' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 44** ! Function 'findByName' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 44** ! Public function 'findByName()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/opensource/mql4-lib/UI/UIRoot.mqh (4 issues)

- [ ] **Line 15** !! Class 'UIRoot' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 15** ! Class 'UIRoot' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 36** ! Parameter 'chart' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */
- [ ] **Line 36** ! Parameter 'subwindow' is never used in function body `UnusedParameter`
  - Fix: Remove unused parameter or prefix with /* unused */

### src/test/resources/samples/Indicators/Awesome.mq4 (4 issues)

- [ ] **Line 29** !! OnInit() should return 'int' (not 'void') to report initialization status `MissingOnInitReturn`
  - Fix: Change return type to 'int'
- [ ] **Line 51** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 51** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/Indicators/Heiken Ashi.mq4 (4 issues)

- [ ] **Line 37** !! OnInit() should return 'int' (not 'void') to report initialization status `MissingOnInitReturn`
  - Fix: Change return type to 'int'
- [ ] **Line 69** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 69** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/Indicators/Alligator.mq4 (4 issues)

- [ ] **Line 31** !! OnInit() should return 'int' (not 'void') to report initialization status `MissingOnInitReturn`
  - Fix: Change return type to 'int'
- [ ] **Line 58** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 58** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/Indicators/Bears.mq4 (4 issues)

- [ ] **Line 23** !! OnInit() should return 'int' (not 'void') to report initialization status `MissingOnInitReturn`
  - Fix: Change return type to 'int'
- [ ] **Line 41** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 41** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/Indicators/Bulls.mq4 (4 issues)

- [ ] **Line 23** !! OnInit() should return 'int' (not 'void') to report initialization status `MissingOnInitReturn`
  - Fix: Change return type to 'int'
- [ ] **Line 41** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 41** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/opensource/mql4-lib/Collection/OrderedIntMap.mqh (4 issues)

- [ ] **Line 54** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 12** ! Class 'OrderedIntMap' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 39** ! Function 'increment' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 54** ! Function 'zero' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/Indicators/Accelerator.mq4 (4 issues)

- [ ] **Line 33** !! OnInit() should return 'int' (not 'void') to report initialization status `MissingOnInitReturn`
  - Fix: Change return type to 'int'
- [ ] **Line 59** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 59** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/Indicators/Accumulation.mq4 (4 issues)

- [ ] **Line 20** !! OnInit() should return 'int' (not 'void') to report initialization status `MissingOnInitReturn`
  - Fix: Change return type to 'int'
- [ ] **Line 31** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 31** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/Indicators/Ichimoku.mq4 (4 issues)

- [ ] **Line 37** !! OnInit() should return 'int' (not 'void') to report initialization status `MissingOnInitReturn`
  - Fix: Change return type to 'int'
- [ ] **Line 86** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 86** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/Include/Controls/Panel.mqh (4 issues)

- [ ] **Line 57** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 71** !! User-defined type 'ENUM_BORDER_TYPE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 71** !! Function 'BorderType()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 119** !! Function 'OnChange()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach

### src/test/resources/samples/Include/Controls/WndClient.mqh (4 issues)

- [ ] **Line 96** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 241** !! Function 'Id()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 256** !! Function 'Show()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 276** !! Function 'OnResize()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach

### src/test/resources/samples/Include/Controls/BmpButton.mqh (3 issues)

- [ ] **Line 82** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 250** !! Function 'OnMouseDown()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 260** !! Function 'OnMouseUp()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach

### src/test/resources/samples/Indicators/Momentum.mq4 (3 issues)

- [ ] **Line 45** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 45** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/Scripts/PipeClientPure.mq4 (3 issues)

- [ ] **Line 16** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 16** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 16** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details

### src/test/resources/samples/Indicators/OsMA.mq4 (3 issues)

- [ ] **Line 60** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 60** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/opensource/mql4-lib/Trade/TimeSeriesData.mqh (3 issues)

- [ ] **Line 10** !! Class 'TimeSeriesData' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 10** ! Class 'TimeSeriesData' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 41** ! Function 'updateNewBar' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/Libraries/stdlib.mq4 (3 issues)

- [ ] **Line 208** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version

### src/test/resources/samples/Indicators/MACD.mq4 (3 issues)

- [ ] **Line 61** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 61** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/Indicators/RSI.mq4 (3 issues)

- [ ] **Line 57** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 57** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/Indicators/CCI.mq4 (3 issues)

- [ ] **Line 57** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 57** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/Include/Controls/Button.mqh (3 issues)

- [ ] **Line 66** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 128** !! Function 'OnMouseDown()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 138** !! Function 'OnMouseUp()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach

### src/test/resources/samples/Indicators/ATR.mq4 (3 issues)

- [ ] **Line 51** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 51** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/opensource/mql4-lib/Utils/Price.mqh (3 issues)

- [ ] **Line 10** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 10** !! User-defined type 'ENUM_APPLIED_PRICE' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)
- [ ] **Line 10** ! Function 'getPrice' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/Indicators/Stochastic.mq4 (3 issues)

- [ ] **Line 64** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 64** ! Function 'OnCalculate' has 10 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/samples/opensource/mql4-lib/UI/BaseElement.mqh (2 issues)

- [ ] **Line 14** !! Class 'BaseElement' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 14** ! Class 'BaseElement' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/opensource/mql4-lib/Lang/Integer.mqh (2 issues)

- [ ] **Line 152** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 152** ! Function 'toString' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/Include/Controls/DatePicker.mqh (2 issues)

- [ ] **Line 87** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 157** !! Function 'Show()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach

### src/test/resources/samples/Include/Strings/String.mqh (2 issues)

- [ ] **Line 362** !! Function 'Compare()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 369** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case

### src/test/resources/samples/Include/Controls/WndObj.mqh (2 issues)

- [ ] **Line 71** !! Function 'OnEvent()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 77** !! Switch statement without 'default' case — unhandled values may cause unexpected behavior `MissingDefaultCase`
  - Fix: Add 'default: break;' case

### src/test/resources/samples/Scripts/PipeClient.mq4 (2 issues)

- [ ] **Line 18** !! Array access without ArraySize() check — risk of out-of-bounds access `ArrayAccessWithoutSizeCheck`
  - Fix: Check ArraySize() before accessing: if(ArraySize(arr) > index) { ... }
- [ ] **Line 18** !! ArrayResize() return value should be checked (returns -1 on failure) `ArrayResizeReturnCheck`
  - Fix: Check ArrayResize() return: if(ArrayResize(arr, size) < 0) { Print("Error"); }

### src/test/resources/samples/Include/Files/FileTxt.mqh (2 issues)

- [ ] **Line 38** !! Function 'Open()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach
- [ ] **Line 56** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations

### src/test/resources/samples/opensource/mql4-lib/Trade/DataProvider.mqh (2 issues)

- [ ] **Line 11** !! Class 'DataProvider' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 11** ! Class 'DataProvider' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/opensource/mql4-lib/Trade/EntryAdvisor.mqh (2 issues)

- [ ] **Line 24** !! Class 'EntryAdvisor' has a constructor but no destructor — potential resource leak `MissingDestructor`
  - Fix: Add destructor: ~ClassName() { /* cleanup */ }
- [ ] **Line 24** ! Class 'EntryAdvisor' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/Include/Controls/Label.mqh (1 issues)

- [ ] **Line 52** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach

### src/test/resources/samples/Include/Indicators/Series.mqh (1 issues)

- [ ] **Line 69** !! User-defined type 'ENUM_TIMEFRAMES' passed by value — use pass-by-reference (&) to avoid unnecessary copy `LargeStructByValue`
  - Fix: Pass large struct by reference: void Func(const MqlTradeRequest &request)

### src/test/resources/samples/Include/Controls/RadioButton.mqh (1 issues)

- [ ] **Line 71** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach

### src/test/resources/samples/opensource/mql4-lib/Lang/Native.mqh (1 issues)

- [ ] **Line 96** !! Function 'StringFromUtf8Pointer()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach

### src/test/resources/samples/Include/Controls/SpinEdit.mqh (1 issues)

- [ ] **Line 85** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach

### src/test/resources/samples/Include/Controls/CheckBox.mqh (1 issues)

- [ ] **Line 79** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach

### src/test/resources/samples/Include/Controls/Scrolls.mqh (1 issues)

- [ ] **Line 99** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach

### src/test/resources/samples/Include/Controls/Picture.mqh (1 issues)

- [ ] **Line 56** !! Function 'Create()' appears to call itself recursively without depth limit `StackOverflowRisk`
  - Fix: Add recursion depth limit or convert to iterative approach

### src/test/resources/parser/functions/FunctionDeclaration.mq4 (24 issues)

- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 3** ! Contains 'TODO' marker — address before production deployment `TodoFixme`
  - Fix: Resolve the TODO/FIXME item or create a tracking issue
- [ ] **Line 4** ! Function 'z2' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 5** ! Function 'z3' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 5** ! Public function 'z3()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 6** ! Function 'z4' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 6** ! Public function 'z4()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 7** ! Function 'z5' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 7** ! Public function 'z5()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 8** ! Function 'z6' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 8** ! Public function 'z6()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 9** ! Function 'z7' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 9** ! Public function 'z7()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 10** ! Function 'z8' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 10** ! Public function 'z8()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 11** ! Function 'z9' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 11** ! Public function 'z9()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 12** ! Function 'z10' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 12** ! Public function 'z10()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 13** ! Function 'z100' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 13** ! Public function 'z100()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 31** ! Contains 'TODO' marker — address before production deployment `TodoFixme`
  - Fix: Resolve the TODO/FIXME item or create a tracking issue
- [ ] **Line 32** ! Contains 'TODO' marker — address before production deployment `TodoFixme`
  - Fix: Resolve the TODO/FIXME item or create a tracking issue

### src/test/resources/parser/classes/Inheritance.mq4 (12 issues)

- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 2** ! Class 'E' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 4** ! Class 'E' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 6** ! Class 'E' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 11** ! Class 'A' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 15** ! Class 'C' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 17** ! Class 'D' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 19** ! Class 'E' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 21** ! Class 'F' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 23** ! Class 'G' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 25** ! Class 'H' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/parser/templates/TemplateClasses.mqh (10 issues)

- [ ] **Line 2** ! Class 'C1' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 3** ! Class 'C2' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 4** ! Class 'C3' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 5** ! Class 'C4' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 6** ! Class 'C5' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 8** ! Class 'C6' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 9** ! Class 'C7' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 10** ! Class 'C8' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 11** ! Class 'C9' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 12** ! Class 'C10' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/Experts/tradingexpert.mq4 (8 issues)

- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 20** ! Trade operation without an IsTradeAllowed()/IsTradeContextBusy() check — the trade context may be busy `MissingTradeContextCheck`
- [ ] **Line 20** ! Function 'start' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 20** ! Multiple GetLastError() calls — consider a centralized error handling approach `OverComplexErrorHandling`
  - Fix: Simplify nested error checks — extract into helper function
- [ ] **Line 20** ! Price comparison without NormalizeDouble() — floating point precision issues `DataConsistency`
  - Fix: Use NormalizeDouble() for price comparisons: if(NormalizeDouble(a-b, _Digits) == 0)
- [ ] **Line 195** ! Function 'Fun_Error' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 233** ! Function 'New_Stop' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/opensource/mql4-lib/Trade/OrderPool.mqh (5 issues)

- [ ] **Line 14** ! Class 'OrderPool' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 27** ! Parameter 'matcher' is a large type passed by reference — consider adding 'const' `MissingConstParameter`
  - Fix: Add 'const' qualifier to reference parameter: const Type &param
- [ ] **Line 27** ! Function 'filter' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 44** ! Class 'HistoryPool' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 53** ! Class 'TradingPool' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/parser/classes/Classes.mq4 (5 issues)

- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 4** ! Class 'DECL' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 6** ! Class 'A' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 8** ! Class 'A' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/Include/ChartObjects/ChartObjectsShapes.mqh (5 issues)

- [ ] **Line 41** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 85** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 85** ! Function 'Create' has 9 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct
- [ ] **Line 130** ! Handle-returning function used without INVALID_HANDLE check `SecureCodingPatterns`
  - Fix: Apply secure coding pattern — see inspection description for details
- [ ] **Line 130** ! Function 'Create' has 9 parameters — consider grouping related parameters into a struct `TooManyParameters`
  - Fix: Group related parameters into a struct

### src/test/resources/parser/classes/Interfaces.mq4 (4 issues)

- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 4** ! Class 'DECL' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 5** ! Class 'A' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/opensource/mql4-lib/Lang/Mql.mqh (4 issues)

- [ ] **Line 23** ! Class 'Callable' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 31** ! Class 'EqualityComparer' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 40** ! Class 'GenericEqualityComparer' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 49** ! Class 'Mql' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/parser/statements/Robustness.mq4 (3 issues)

- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version
- [ ] **Line 20** ! Public function 'AfterRecovery()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/Include/Arrays/Array.mqh (3 issues)

- [ ] **Line 112** ! File read operation without error checking — check return values `FileOperationValidation`
  - Fix: Check return value of file read/write operations
- [ ] **Line 132** ! Public function 'Minimum()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment
- [ ] **Line 153** ! Public function 'Maximum()' lacks a documentation comment `MissingFunctionDocComment`
  - Fix: Insert documentation comment

### src/test/resources/samples/opensource/mql4-lib/Trade/Terminal.mqh (3 issues)

- [ ] **Line 12** ! Class 'Terminal' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 52** ! Function 'notify' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()
- [ ] **Line 80** ! Function 'mail' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/samples/opensource/mql4-lib/Lang/Object.mqh (2 issues)

- [ ] **Line 13** ! Class 'Object' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 25** ! Function 'hash' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/parser/preprocessor/Include.mq4 (2 issues)

- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version

### src/test/resources/samples/opensource/mql4-lib/Charts/Fib.mqh (2 issues)

- [ ] **Line 10** ! Class 'Fib' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 28** ! Function 'calc' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/parser/preprocessor/Undef.mq4 (2 issues)

- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version

### src/test/resources/parser/expressions/SizeOf.mq4 (2 issues)

- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version

### src/test/resources/parser/statements/Enum.mq4 (2 issues)

- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version

### src/test/resources/samples/opensource/mql4-lib/Trade/Filter.mqh (2 issues)

- [ ] **Line 13** ! Class 'Filter' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 23** ! Class 'FilterAdapter' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/parser/classes/Structs.mq4 (2 issues)

- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version

### src/test/resources/parser/preprocessor/Define.mq4 (2 issues)

- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version

### src/test/resources/parser/comments/LineComments.mq4 (2 issues)

- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version

### src/test/resources/samples/opensource/mql4-lib/Trade/Trigger.mqh (2 issues)

- [ ] **Line 12** ! Class 'Trigger' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 21** ! Class 'TriggerAdapter' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/parser/preprocessor/Ifdef.mq4 (2 issues)

- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version

### src/test/resources/samples/opensource/mql4-lib/Collection/Map.mqh (2 issues)

- [ ] **Line 13** ! Class 'MapIterator' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 24** ! Class 'Map' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/opensource/mql4-lib/Charts/Pivot.mqh (2 issues)

- [ ] **Line 10** ! Class 'Pivot' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass
- [ ] **Line 30** ! Function 'calc' should use PascalCase naming convention `FunctionNamingConvention`
  - Fix: Rename function to PascalCase: MyFunctionName()

### src/test/resources/parser/comments/BlockComments.mq4 (2 issues)

- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version

### src/test/resources/parser/preprocessor/PropertyColorList.mq4 (2 issues)

- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description
- [ ] **Line 1** ! Missing '#property version' — add a version for the script/indicator/EA `MissingPropertyVersion`
  - Fix: Insert #property version

### src/test/resources/samples/opensource/mql4-lib/Charts/Chart.mqh (1 issues)

- [ ] **Line 12** ! Class 'Chart' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/opensource/mql4-lib/Lang/ExpertAdvisor.mqh (1 issues)

- [ ] **Line 17** ! Class 'ExpertAdvisor' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/opensource/mql4-lib/Lang/EventApp.mqh (1 issues)

- [ ] **Line 27** ! Class 'EventApp' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/opensource/mql4-lib/Charts/Fractal.mqh (1 issues)

- [ ] **Line 12** ! Class 'Fractal' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/opensource/mql4-lib/Utils/Math.mqh (1 issues)

- [ ] **Line 10** ! Class 'Math' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/opensource/mql4-lib/UI/Mouse.mqh (1 issues)

- [ ] **Line 19** ! Class 'Mouse' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/Indicators/Examples/SimplePanel.mq4 (1 issues)

- [ ] **Line 1** ! Missing '#property description' — add a description for the script/indicator/EA `MissingPropertyDescription`
  - Fix: Insert #property description

### src/test/resources/samples/opensource/mql4-lib/Trade/Account.mqh (1 issues)

- [ ] **Line 10** ! Class 'Account' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/opensource/mql4-lib/Utils/Compare.mqh (1 issues)

- [ ] **Line 10** ! Class 'Compare' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/opensource/mql4-lib/Lang/Script.mqh (1 issues)

- [ ] **Line 16** ! Class 'Script' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

### src/test/resources/samples/opensource/mql4-lib/Lang/Indicator.mqh (1 issues)

- [ ] **Line 17** ! Class 'Indicator' should follow CClassName convention (start with 'C' prefix) `ClassNamingConvention`
  - Fix: Rename class with 'C' prefix: class CMyClass

## Clean Files

- [x] ~~src/test/resources/samples/opensource/mql4-lib/OpenCL/Definitions.mqh~~
- [x] ~~src/test/resources/samples/opensource/mql4-lib/Lang/Event.mqh~~
- [x] ~~src/test/resources/samples/Include/Controls/Defines.mqh~~
- [x] ~~src/test/resources/samples/Include/StdLibErr.mqh~~
- [x] ~~src/test/resources/samples/opensource/mql4-lib/OpenCL/MQL5.mqh~~
- [x] ~~src/test/resources/samples/Include/stderror.mqh~~
- [x] ~~src/test/resources/samples/opensource/mql4-lib/Lang/String.mqh~~
- [x] ~~src/test/resources/samples/Include/stdlib.mqh~~
- [x] ~~src/test/resources/samples/Include/Controls/Rect.mqh~~
- [x] ~~src/test/resources/samples/Include/WinUser32.mqh~~
- [x] ~~src/test/resources/samples/Include/Object.mqh~~
